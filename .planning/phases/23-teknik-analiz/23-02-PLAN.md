---
phase: 23-teknik-analiz
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - src/lib/math/technical-indicators.ts
  - src/lib/math/technical-indicators.test.ts
  - src/components/dashboard/report/PriceChart.tsx
autonomous: true
requirements: ["TA-02"]
must_haves:
  truths:
    - "Volume chart highlights days with >2x average volume in orange/red"
    - "Tooltip explicitly states 'Anormal Hacim' on highlighted days"
  artifacts:
    - path: "src/components/dashboard/report/PriceChart.tsx"
      provides: "Volume Bar with conditional cell coloring"
  key_links:
    - from: "src/components/dashboard/report/PriceChart.tsx"
      to: "volume bar"
      via: "Recharts Cell coloring based on average"
      pattern: "<Cell.*fill"
---

<objective>
Hacim çubuğu grafiğini PriceChart içerisine entegre ederek, ortalama hacmin 2 katını aşan anormal işlem günlerini görsel olarak vurgulamak.
Purpose: Kullanıcının fiyat hareketlerindeki hacim destekli kırılımları/anomalileri hızla fark edebilmesini sağlamak.
Output: PriceChart'ta ikincil Y ekseniyle çalışan hacim barları ve anomali vurgusu.
</objective>

<execution_context>
@C:/Users/duyma/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/duyma/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/23-teknik-analiz/23-PATTERNS.md
@.planning/phases/23-teknik-analiz/23-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Volume Average Utility</name>
  <files>src/lib/math/technical-indicators.ts, src/lib/math/technical-indicators.test.ts</files>
  <behavior>
    - Test calculateVolumeSMA: Should correctly compute a moving average for the volume array with the specified period (e.g., 20 days).
  </behavior>
  <action>
    Add `calculateVolumeSMA(data: number[], period: number = 20)` to `technical-indicators.ts`. This functions identically to `calculateSMA` but is semantically named and can have a different default period. (Or reuse calculateSMA if exactly identical).
  </action>
  <verify>
    <automated>npx vitest run src/lib/math/technical-indicators.test.ts</automated>
  </verify>
  <done>Volume average function is implemented and tested.</done>
</task>

<task type="auto">
  <name>Task 2: Render Volume Anomaly Visualization</name>
  <files>src/components/dashboard/report/PriceChart.tsx</files>
  <action>
    1. In `PriceChart.tsx`'s `ComposedChart`, add a secondary `<YAxis>` for volume (yAxisId="volume", orientation="right", optionally hidden so it doesn't clutter).
    2. Add `<Bar yAxisId="volume" dataKey="volume">` to render the volume.
    3. Iterate over the charted data to populate an `avgVolume` property using the utility from Task 1.
    4. Inside the `<Bar>`, map the data to `<Cell>` components. If `entry.volume > entry.avgVolume * 2`, set fill to `#ea580c` (orange), else `#22c55e30` (greenish low opacity).
    5. Update the custom Recharts Tooltip content to check for the anomaly condition. If true, display an additional row in the tooltip: "Anormal Hacim: [X]" with a distinct text color (e.g. red).
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>Volume bars are rendered behind the price chart, anomalous days are colored orange, and the tooltip shows the "Anormal Hacim" warning.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Component Rendering | Display logic runs client-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-02 | Denial of Service | PriceChart.tsx | accept | Volume calculations are simple arrays; standard browser JS engines can handle ~500 points trivially |
</threat_model>

<verification>
- Volume bars appear at the bottom of the chart
- Anomaly coloring condition is mathematically correct (> 2x average)
</verification>

<success_criteria>
- Linter passes.
- Volume visually highlights spikes.
- Tooltip shows accurate volume anomaly info.
</success_criteria>

<output>
After completion, create `.planning/phases/23-teknik-analiz/23-02-SUMMARY.md`
</output>