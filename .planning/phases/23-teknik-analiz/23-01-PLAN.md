---
phase: 23-teknik-analiz
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/market.ts
  - src/lib/market/yahoo-client.ts
  - src/lib/market/client.ts
  - src/lib/market/providers/yahoo.ts
  - src/agents/market-agent.ts
  - src/app/dashboard/history/[id]/page.tsx
  - src/components/dashboard/report/PriceChart.tsx
  - src/components/dashboard/report/RsiChart.tsx
  - src/lib/market/technical-indicators.ts
  - src/lib/market/technical-indicators.test.ts
autonomous: true
requirements: ["TA-01"]
must_haves:
  truths:
    - "MA20, MA50, and MA200 overlays are toggleable on the price chart"
    - "RSI(14) panel is displayed below the price chart and synchronized via tooltips"
    - "Client-side calculations use 2 years of daily data from the backend to ensure MA200 works"
  artifacts:
    - path: "src/lib/math/technical-indicators.ts"
      provides: "Client-side Technical indicator formulas"
      exports: ["calculateSMA", "calculateRSI"]
    - path: "src/components/dashboard/report/RsiChart.tsx"
      provides: "Synchronized RSI panel"
      contains: "<ComposedChart"
  key_links:
    - from: "src/components/dashboard/report/PriceChart.tsx"
      to: "src/components/dashboard/report/RsiChart.tsx"
      via: "syncId prop for synchronized hovering"
      pattern: "syncId"
    - from: "src/lib/market/yahoo-client.ts"
      to: "src/types/market.ts"
      via: "daily data generation"
      pattern: "interval: '1d'"
---

<objective>
PriceChart üzerine MA20/MA50/MA200 overlay'leri eklemek, ayrı bir RSI paneli oluşturmak ve bunları destekleyecek günlük veriyi (BIST100 dahil) backend'den sağlamak.
Purpose: Kullanıcının temel teknik analiz göstergelerini (trend ve momentum) görsel olarak değerlendirebilmesini sağlamak.
Output: Güncellenmiş veri modeli, MA ve RSI hesaplama yardımcı fonksiyonları, ComposedChart tabanlı PriceChart ve yeni RsiChart.
</objective>

<execution_context>
@C:/Users/duyma/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/duyma/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/23-teknik-analiz/23-PATTERNS.md
@.planning/phases/23-teknik-analiz/23-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Market Data Model & Fetching for Daily Data</name>
  <files>src/types/market.ts, src/lib/market/yahoo-client.ts, src/lib/market/client.ts, src/lib/market/providers/yahoo.ts, src/agents/market-agent.ts, src/app/dashboard/history/[id]/page.tsx, src/components/dashboard/report/PriceChart.tsx</files>
  <action>
    1. In `src/types/market.ts`, rename `monthlySeries` to `timeSeries` and `MonthlySeriesEntry` to `TimeSeriesEntry`. Add `benchmarkSeries?: Record<string, TimeSeriesEntry>` to the `MarketData` interface.
    2. In `src/lib/market/yahoo-client.ts`, modify the `yf.chart` call for the main ticker to use `interval: '1d'` (daily) with a 2-year lookback (`period1` = 2 years ago, `period2` = now) to enable MA200 computation.
    3. In `src/lib/market/yahoo-client.ts`, add a concurrent `yf.chart` call to fetch `XU100.IS` for the same 2-year daily period and assign it to the new `benchmarkSeries` property.
    4. Replace all occurrences of `monthlySeries` with `timeSeries` and `MonthlySeriesEntry` with `TimeSeriesEntry` across the rest of the specified files so the build passes.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>MarketData model and Yahoo client use daily data and include benchmarkSeries, with no TypeScript compilation errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Math Utilities for Technical Indicators</name>
  <files>src/lib/math/technical-indicators.ts, src/lib/math/technical-indicators.test.ts</files>
  <behavior>
    - Test calculateSMA: Should return array of identical length, with `null` for indices < period - 1, and correct averages otherwise.
    - Test calculateRSI: Should calculate correct 14-period RSI, mapping early values to `null`.
  </behavior>
  <action>
    Implement pure TypeScript functions: `calculateSMA(data: number[], period: number)` and `calculateRSI(data: number[], period: number)`. Ensure they handle empty arrays and periods larger than the dataset gracefully.
  </action>
  <verify>
    <automated>npx vitest run src/lib/math/technical-indicators.test.ts</automated>
  </verify>
  <done>Math utilities correctly compute SMA and RSI with 100% test passing.</done>
</task>

<task type="auto">
  <name>Task 3: Implement MA Overlays and Synchronized RSI Panel</name>
  <files>src/components/dashboard/report/PriceChart.tsx, src/components/dashboard/report/RsiChart.tsx, src/app/dashboard/history/[id]/page.tsx</files>
  <action>
    1. Create `src/components/dashboard/report/RsiChart.tsx` using Recharts `<ComposedChart>`. It receives `timeSeries` data, calculates RSI(14) using the new math utility, and renders a `<Line>` with `<ReferenceLine>`s at 30 (green) and 70 (red). Use `syncId="tech-analysis-chart"`.
    2. Refactor `PriceChart.tsx` to use `<ComposedChart>` instead of `<AreaChart>`. Add state/toggles for MA20, MA50, MA200 (MA50 default `true`).
    3. Calculate MAs on the full 2-year dataset to ensure MA200 is valid, THEN slice the rendered data down to the most recent 12 months for the chart display so it's not overcrowded.
    4. Render `MA` overlays as `<Line>` components in `PriceChart`. Set `syncId="tech-analysis-chart"`.
    5. In `src/app/dashboard/history/[id]/page.tsx`, render `RsiChart` directly below `PriceChart`.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>PriceChart contains MA toggles and lines; RsiChart is rendered below it; components are wired using syncId.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client Browser | Technical indicator calculations execute client-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Denial of Service | technical-indicators.ts | mitigate | Ensure math functions don't infinite loop and handle empty/malformed arrays securely |
</threat_model>

<verification>
- `MarketData` model has `timeSeries` instead of `monthlySeries`
- Technical indicators calculation runs purely client-side via TS functions
- `PriceChart` and `RsiChart` are linked by Recharts `syncId`
</verification>

<success_criteria>
- Build and linter pass.
- No new external chart/math libraries are added.
- PriceChart has toggleable MAs and RsiChart has 30/70 reference lines.
</success_criteria>

<output>
After completion, create `.planning/phases/23-teknik-analiz/23-01-SUMMARY.md`
</output>
