---
phase: 23-teknik-analiz
plan: 03
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - src/components/dashboard/report/RelativePerformance.tsx
  - src/app/dashboard/history/[id]/page.tsx
autonomous: true
requirements: ["TA-03"]
must_haves:
  truths:
    - "BIST100 performance comparison is visible for the analyzed ticker"
  artifacts:
    - path: "src/components/dashboard/report/RelativePerformance.tsx"
      provides: "Comparison UI component"
      exports: ["RelativePerformance"]
  key_links:
    - from: "src/app/dashboard/history/[id]/page.tsx"
      to: "RelativePerformance"
      via: "renders with benchmarkSeries data"
      pattern: "<RelativePerformance"
---

<objective>
BIST100 relative performance karşılaştırmasını hesaplayıp raporda göstermek.
Purpose: Hissenin incelenen dönemde endekse göre nasıl bir performans sergilediğini özetlemek.
Output: RelativePerformance React bileşeni ve history page entegrasyonu.
</objective>

<execution_context>
@C:/Users/duyma/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/duyma/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/23-teknik-analiz/23-PATTERNS.md
@.planning/phases/23-teknik-analiz/23-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RelativePerformance Component</name>
  <files>src/components/dashboard/report/RelativePerformance.tsx</files>
  <action>
    Create a new component `RelativePerformance.tsx`. It should accept `ticker: string`, `timeSeries`, and `benchmarkSeries`.
    Calculate the percentage change from the first available date to the last available date for both the specific ticker and the BIST100 benchmark.
    Render a simple UI element (like a small badge or card) that displays the comparison: e.g., "ASELSAN +91% | BIST100 +42%". Add styling to indicate outperformance (green) or underperformance (red/neutral) compared to the benchmark.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>Component computes the relative percentage differences and renders correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate RelativePerformance into Dashboard</name>
  <files>src/app/dashboard/history/[id]/page.tsx</files>
  <action>
    Import the new `RelativePerformance` component.
    Render it inside the report view. A good placement is near the `PriceChart` header or inside the "Row 1.5: Company Info Banner" where other high-level metrics are displayed.
    Pass `ticker`, `timeSeries` (formerly `monthlySeries`), and `benchmarkSeries` to the component.
    Ensure safe fallback if `benchmarkSeries` is null or missing (e.g., render nothing or a generic placeholder).
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>RelativePerformance component is embedded in the report UI without breaking existing layout.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Component Rendering | Display logic runs client-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-03 | Information Disclosure | RelativePerformance.tsx | accept | UI simply displays math based on public data already fetched |
</threat_model>

<verification>
- `RelativePerformance` renders on the page.
- Both ticker and benchmark change percentages are calculated from the same start and end dates.
</verification>

<success_criteria>
- Build passes.
- Component accurately displays the comparative return over the dataset's visible window.
</success_criteria>

<output>
After completion, create `.planning/phases/23-teknik-analiz/23-03-SUMMARY.md`
</output>