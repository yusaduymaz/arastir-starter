---
phase: "05"
plan: "03"
type: execute
wave: 3
depends_on: ["05-02"]
files_modified: ["src/app/dashboard/history/page.tsx", "src/components/dashboard/HistoryTable.tsx"]
autonomous: true
requirements: ["UI-02", "UI-04"]

must_haves:
  truths:
    - "Authenticated users can see their past research requests."
    - "Users can download completed reports."
---

<objective>
Develop the results page and history view for authenticated users.
</objective>

<execution_context>
@C:/Users/duyma/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/05-user-facing-application/05-RESEARCH.md
@.planning/phases/05-user-facing-application/05-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build History Table Component</name>
  <files>src/components/dashboard/HistoryTable.tsx</files>
  <action>
    Create `src/components/dashboard/HistoryTable.tsx`.
    Implement the table as defined in `05-UI-SPEC.md`. It should take an array of `research_history` records as props.
    Display columns for Date, Query, Status, and Actions.
    If status is 'completed', render download buttons for the PDF/PPTX using the `result_path` URLs.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>HistoryTable component renders correctly based on status.</done>
</task>

<task type="auto">
  <name>Task 2: Implement History View Page</name>
  <files>src/app/dashboard/history/page.tsx, src/app/dashboard/page.tsx</files>
  <action>
    Update `src/app/dashboard/page.tsx` to include the `HistoryTable` component, fetching the user's recent history on the server-side and passing it down.
    (Optional) Create a dedicated `src/app/dashboard/history/page.tsx` for a full-page view if needed, otherwise integrate it fully into the main dashboard page as per the spec.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>History data is fetched from Supabase and rendered in the UI.</done>
</task>

</tasks>

<success_criteria>
- The dashboard displays a list of past research requests.
- Users can click to download reports for completed requests.
</success_criteria>

<output>
After completion, create `.planning/phases/05-user-facing-application/05-03-SUMMARY.md`
</output>