---
phase: "05"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: ["src/lib/supabase/server.ts", "src/lib/supabase/middleware.ts", "src/middleware.ts", "src/app/(auth)/login/page.tsx", "src/app/(auth)/register/page.tsx"]
autonomous: true
requirements: ["UI-03"]

must_haves:
  truths:
    - "A user can register and log in."
    - "Protected routes redirect unauthenticated users."
  artifacts:
    - path: "src/lib/supabase/server.ts"
      provides: "Server-side Supabase client"
    - path: "src/app/(auth)/login/page.tsx"
      provides: "Login UI"
---

<objective>
Implement user registration and login functionality using Supabase Auth SSR.
</objective>

<execution_context>
@C:/Users/duyma/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/05-user-facing-application/05-RESEARCH.md
@.planning/phases/05-user-facing-application/05-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup Supabase SSR Clients</name>
  <files>src/lib/supabase/server.ts, src/lib/supabase/middleware.ts</files>
  <action>
    Install `@supabase/ssr`.
    Create `src/lib/supabase/server.ts` to export a `createClient()` function that initializes the Supabase client using cookies from `next/headers`.
    Create `src/lib/supabase/middleware.ts` to export a function that updates the session cookie in the Next.js middleware response.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Supabase SSR clients are configured.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Route Protection</name>
  <files>src/middleware.ts</files>
  <action>
    Create `src/middleware.ts` at the root.
    Use the `updateSession` function from `src/lib/supabase/middleware.ts`.
    Configure the matcher to run on all routes except static assets and API routes.
    Redirect unauthenticated users accessing `/dashboard` to `/login`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Middleware protects the dashboard route.</done>
</task>

<task type="auto">
  <name>Task 3: Build Auth UIs</name>
  <files>src/app/(auth)/login/page.tsx, src/app/(auth)/register/page.tsx</files>
  <action>
    Create the login and registration pages using Tailwind CSS as specified in `05-UI-SPEC.md`.
    Implement standard HTML forms that submit to Next.js Server Actions to handle `supabase.auth.signInWithPassword` and `supabase.auth.signUp`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Auth pages are built and functional.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Spoofing | Auth | mitigate | Utilize official `@supabase/ssr` to securely handle JWTs in HttpOnly cookies. |
</threat_model>

<success_criteria>
- Unauthenticated users cannot access `/dashboard`.
- Users can log in using email/password.
</success_criteria>

<output>
After completion, create `.planning/phases/05-user-facing-application/05-01-SUMMARY.md`
</output>