---
phase: "05"
plan: "02"
type: execute
wave: 2
depends_on: ["05-01"]
files_modified: ["src/app/dashboard/page.tsx", "src/app/api/research/route.ts"]
autonomous: true
requirements: ["UI-01"]

must_haves:
  truths:
    - "Users can submit a research query via the dashboard."
    - "The system creates a pending record in the database."
---

<objective>
Develop the main dashboard page and the asynchronous API route to trigger research agents.
</objective>

<execution_context>
@C:/Users/duyma/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/05-user-facing-application/05-RESEARCH.md
@.planning/phases/05-user-facing-application/05-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Dashboard Layout</name>
  <files>src/app/dashboard/page.tsx</files>
  <action>
    Create `src/app/dashboard/page.tsx`.
    Implement the core layout (Top Navigation, Main Content Area) and the Query Form as defined in `05-UI-SPEC.md`.
    The form should include an input for the ticker and a submit button.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Dashboard layout and form are implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Research API Route</name>
  <files>src/app/api/research/route.ts</files>
  <action>
    Create `src/app/api/research/route.ts`.
    Implement a POST handler that:
    1. Authenticates the user via `createClient()`.
    2. Inserts a new row into `research_history` (user_id, query, status='in_progress').
    3. Asynchronously invokes a mock pipeline execution function (fire-and-forget).
    4. Returns a 202 Accepted response with the record ID.
    Update the Dashboard Client component to submit to this API route.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>API route securely handles queries and returns immediately.</done>
</task>

</tasks>

<success_criteria>
- A user can enter a ticker on the dashboard and submit it.
- A new row appears in the `research_history` database table.
</success_criteria>

<output>
After completion, create `.planning/phases/05-user-facing-application/05-02-SUMMARY.md`
</output>