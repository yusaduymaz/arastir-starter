---
phase: 02-core-reporting-engine
plan: 03
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified: ["src/agents/writer-agent.ts", "src/agents/run-writer.ts"]
autonomous: true
requirements: ["AGENT-01"]
must_haves:
  truths:
    - "Writer agent orchestrates simultaneous generation of PDF and PPTX."
    - "Outputs are properly saved to an `outputs/` directory."
  artifacts:
    - path: "src/agents/writer-agent.ts"
      provides: "Main logic orchestrating the reporting step."
    - path: "src/agents/run-writer.ts"
      provides: "CLI script to execute the writer agent."
  key_links:
    - from: "src/agents/writer-agent.ts"
      to: "src/lib/pdf/generator.ts"
      via: "Concurrent execution with Promise.all"
    - from: "src/agents/writer-agent.ts"
      to: "src/lib/pptx/generator.ts"
      via: "Concurrent execution with Promise.all"
---

<objective>
Create the basic Writer Agent that orchestrates the document generation process by concurrently calling the PDF and PPTX generator utilities.

Purpose: Stitches together the individual utility modules to act on a given dataset, realizing the end-to-end reporting pipeline.
Output: `writer-agent.ts` service and `run-writer.ts` execution script.
</objective>

<context>
@.planning/ROADMAP.md
@src/types/research.ts
@src/lib/pdf/generator.ts
@src/lib/pptx/generator.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Writer Agent Logic</name>
  <files>src/agents/writer-agent.ts</files>
  <action>Create and export a `runWriterAgent(data: ReportData, outputDir: string)` function.
- As per `02-PATTERNS.md`, structure the logic similarly to analogous orchestrator functions, handling the concurrent processes safely.
- Import `fs` and `path` modules.
- Ensure the `outputDir` exists using `fs.mkdirSync(outputDir, { recursive: true })` or async equivalent.
- Concurrently invoke `generatePdf` and `generatePptx` using `Promise.all()`.
- Pass correctly joined paths for each generator (e.g., `path.join(outputDir, 'report.pdf')` and `path.join(outputDir, 'presentation.pptx')`).
- Add informative console logs indicating generation status ("Generating reports...", "Finished").</action>
  <verify>
    <automated>npx tsc --noEmit src/agents/writer-agent.ts</automated>
  </verify>
  <done>Agent logic handles directory creation and concurrent document generation without blocking.</done>
</task>

<task type="auto">
  <name>Task 2: Implement CLI Runner for Writer Agent</name>
  <files>src/agents/run-writer.ts</files>
  <action>Create a CLI execution script to run the Writer Agent directly.
- As per `02-PATTERNS.md`, use the `test-tcmb-agent.ts` structure of using an async main function with logging and catching at the top level to exit.
- Use `fs.readFileSync` to read an actual JSON output file from the `research/` directory (e.g., `research/sample-data.json`) and parse it into a `ReportData` object.
- Generate a dynamic output directory path using a timestamp and the report title as per GEMINI.md convention: `outputs/${Date.now()}-${data.title.replace(/\s+/g, '-').toLowerCase()}/`.
- Call `runWriterAgent` within a `try/catch` block.
- On error, `console.error` and `process.exit(1)`.
- On success, log the final output directory path.</action>
  <verify>
    <automated>npx tsc --noEmit src/agents/run-writer.ts</automated>
  </verify>
  <done>A standalone script exists that verifies the end-to-end reporting pipeline locally.</done>
</task>

</tasks>

<success_criteria>
- The Writer Agent successfully uses both generation modules in parallel.
- The `run-writer.ts` script can be executed directly to generate physical test files in the `outputs/` directory.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-reporting-engine/02-03-SUMMARY.md`
</output>
