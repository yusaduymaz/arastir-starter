---
phase: 02-core-reporting-engine
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: ["src/lib/pptx/generator.ts"]
autonomous: true
requirements: ["REPORT-02"]
must_haves:
  truths:
    - "System can generate a PPTX file from structured JSON data."
    - "PPTX contains a title slide and at least one data slide."
  artifacts:
    - path: "src/lib/pptx/generator.ts"
      provides: "PPTX generation utility using pptxgenjs."
  key_links:
    - from: "src/lib/pptx/generator.ts"
      to: "pptxgenjs"
      via: "PPTX slide creation and export"
---

<objective>
Implement the PPTX generation module using `pptxgenjs` to convert structured ReportData into a presentation file.

Purpose: Delivers the capability to produce the final PPTX presentation artifact.
Output: PPTX generator module (`src/lib/pptx/generator.ts`).
</objective>

<context>
@.planning/ROADMAP.md
@src/types/research.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement PPTX Generator Module</name>
  <files>src/lib/pptx/generator.ts</files>
  <action>Create a module that exports a `generatePptx(data: ReportData, outputPath: string)` function.
- As defined in `02-PATTERNS.md`, use the `try...catch` pattern seen in analogous client files like `src/lib/kap/client.ts` for error handling.
- Initialize a new `PptxGenJS` presentation.
- Create a Title Slide displaying `data.title`, `data.source`, and `data.dateGenerated` using `slide.addText()`.
- Create a Data Slide that visualizes `data.data`. Attempt to add a simple chart if the values are numeric (`slide.addChart(pptx.charts.BAR, chartData)`), otherwise map them to a table or text block.
- Map the data correctly to `{ name, labels, values }` format for the chart if possible.
- Save the presentation using `pptx.writeFile({ fileName: outputPath })`.
- Ensure explicit `try...catch` handling as outlined in `02-PATTERNS.md`.</action>
  <verify>
    <automated>npx tsc --noEmit src/lib/pptx/generator.ts</automated>
  </verify>
  <done>Module compiles cleanly and implements robust PPTX generation logic with title and data slides.</done>
</task>

</tasks>

<success_criteria>
- `generatePptx` function successfully builds and exports a `.pptx` file.
- Basic error handling is included around the file write operation.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-reporting-engine/02-02-SUMMARY.md`
</output>
