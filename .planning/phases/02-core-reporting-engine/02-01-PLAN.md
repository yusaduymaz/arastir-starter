---
phase: 02-core-reporting-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["src/lib/pdf/generator.ts"]
autonomous: true
requirements: ["REPORT-01"]
must_haves:
  truths:
    - "System can generate a PDF file from structured JSON data."
    - "PDF correctly renders basic HTML containing the data."
  artifacts:
    - path: "src/lib/pdf/generator.ts"
      provides: "PDF generation utility using Puppeteer."
  key_links:
    - from: "src/lib/pdf/generator.ts"
      to: "puppeteer"
      via: "page.setContent and page.pdf"
---

<objective>
Implement the PDF generation module using Puppeteer to convert structured ReportData into a PDF file.

Purpose: Delivers the capability to produce the final PDF artifact.
Output: PDF generator module (`src/lib/pdf/generator.ts`).
</objective>

<context>
@.planning/ROADMAP.md
@src/types/research.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement PDF Generator Module</name>
  <files>src/lib/pdf/generator.ts</files>
  <action>Create a module that exports a `generatePdf(data: ReportData, outputPath: string)` function.
- As defined in `02-PATTERNS.md`, use the `try...finally` pattern seen in analogous client files like `src/lib/kap/client.ts` for cleanup (e.g. `await browser.close()`).
- Generate a basic HTML template string that includes the `ReportData` (title, source, dateGenerated, and maps the `data` array to an HTML list or table).
- Launch `puppeteer` in headless mode: `puppeteer.launch({ headless: true })`.
- Create a new page and set its content: `page.setContent(htmlString, { waitUntil: 'networkidle0' })`.
- Export to PDF using `page.pdf({ path: outputPath, format: 'A4', printBackground: true })`.
- Use a `try...finally` block to ensure `browser.close()` is always called, preventing zombie processes.</action>
  <verify>
    <automated>npx tsc --noEmit src/lib/pdf/generator.ts</automated>
  </verify>
  <done>Module compiles cleanly and implements robust PDF generation logic with proper browser closure.</done>
</task>

</tasks>

<success_criteria>
- `generatePdf` successfully constructs a PDF from data.
- The `try...finally` pattern matches the existing codebase standards for Puppeteer usage.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-reporting-engine/02-01-SUMMARY.md`
</output>
