---
phase: "04"
plan: "02"
type: execute
wave: 2
depends_on: ["04-01"]
files_modified: ["src/lib/news/client.ts"]
autonomous: true
requirements: ["SCRAPE-02"]

must_haves:
  truths:
    - "The system can retrieve the most recent articles about a specific company from Bloomberg HT."
    - "The main text content of the articles is extracted, cleaned of ads/boilerplate, and stored."
  artifacts:
    - path: "src/lib/news/client.ts"
      provides: "Puppeteer scraper for news"
  key_links:
    - from: "src/lib/news/client.ts"
      to: "Bloomberg HT website"
      via: "Puppeteer with stealth plugin"
---

<objective>
Implement the web scraper to fetch news articles from Bloomberg HT.

Purpose: We need to pull dynamic news content from financial news sites. Bloomberg HT is a good starting point but requires Puppeteer Stealth to bypass bot protections.
Output: A fetching utility function that scrapes search results and parses individual articles.
</objective>

<execution_context>
@C:/Users/duyma/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/duyma/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-high-risk-data-integration-news/04-PATTERNS.md
@.planning/phases/04-high-risk-data-integration-news/04-RESEARCH.md

<interfaces>
From src/types/news.ts (created in 04-01):
```typescript
import { z } from 'zod';

export interface NewsArticle {
  title: string;
  date: string;
  source: string;
  url: string;
  content: string;
  sentiment?: 'positive' | 'negative' | 'neutral';
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Bloomberg HT Scraper</name>
  <files>src/lib/news/bloomberg.ts</files>
  <action>
    Create `src/lib/news/bloomberg.ts`. Implement and export an async function `fetchBloombergNews(query: string, limit: number = 3): Promise<NewsArticle[]>`.
    As explicitly mapped in `04-PATTERNS.md`, this function MUST follow the exact same architectural pattern, error handling (`try...finally`), and StealthPlugin initialization seen in its analog `src/lib/kap/client.ts`.
    1. Navigate to `https://www.bloomberght.com/arama/[query]`. Wait for networkidle2.
    2. Extract up to `limit` valid article links (links containing 'bloomberght.com/haberler' or ending in a number).
    3. Iterate through each link, navigate to it, and extract the text from `.article-content` or `article` tag.
    4. Crucially: remove `<script>`, `<style>`, `iframe`, `.ad`, `.advertisement` elements from the DOM before extracting `textContent`.
    5. Ensure the browser is properly closed in a `finally` block even if scraping fails.
    Return the array of `NewsArticle` objects containing the extracted data.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>src/lib/news/bloomberg.ts successfully compiles and exports the fetchBloombergNews function.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Second News Scraper</name>
  <files>src/lib/news/ekonomim.ts</files>
  <action>
    Create `src/lib/news/ekonomim.ts` (formerly Dünya). Implement and export an async function `fetchEkonomimNews(query: string, limit: number = 3): Promise<NewsArticle[]>`.
    As explicitly mapped in `04-PATTERNS.md`, this function MUST follow the exact same architectural pattern, error handling (`try...finally`), and StealthPlugin initialization seen in its analog `src/lib/kap/client.ts`.
    1. Navigate to `https://www.ekonomim.com/arama?q=[query]`. Wait for networkidle2.
    2. Extract up to `limit` valid article links from the search results.
    3. Iterate through each link, navigate to it, and extract the text from the main article container (e.g. `.content-text`, `article`).
    4. Remove noise (ads, scripts) before extracting `textContent`.
    5. Close browser in a `finally` block.
    Return the array of `NewsArticle` objects.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>src/lib/news/ekonomim.ts successfully compiles and exports the fetchEkonomimNews function.</done>
</task>

<task type="auto">
  <name>Task 3: Create Unified News Client</name>
  <files>src/lib/news/client.ts</files>
  <action>
    Create `src/lib/news/client.ts`. Implement and export an async function `fetchNews(query: string, limit: number = 3): Promise<NewsArticle[]>`.
    As explicitly mapped in `04-PATTERNS.md`, this aggregator MUST follow the exact same architectural pattern, error handling (`try...finally`) seen in its analog `src/lib/kap/client.ts`.
    This function should call both `fetchBloombergNews` and `fetchEkonomimNews` concurrently (using `Promise.all`), merge the results, and return them up to a total combined `limit * 2` (or adjust limit per scraper).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>src/lib/news/client.ts successfully aggregates both news sources.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Web Scraping | Scraping external content from Bloomberg HT introduces untrusted HTML content into the system. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Spoofing/Information Disclosure | src/lib/news/client.ts | mitigate | External site might return malicious scripts. Extracted text should only use textContent, never innerHTML. Puppeteer should run in restricted mode (--no-sandbox, --disable-setuid-sandbox). |
</threat_model>

<verification>
- The client module builds successfully.
</verification>

<success_criteria>
- The scraper is capable of launching headless chrome and fetching articles without being blocked.
</success_criteria>

<output>
After completion, create `.planning/phases/04-high-risk-data-integration-news/04-02-SUMMARY.md`
</output>
