---
phase: "04"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: ["src/types/news.ts", "src/lib/news/sentiment.ts"]
autonomous: true
requirements: ["AGENT-03"]

must_haves:
  truths:
    - "A basic sentiment (positive, negative, neutral) can be assigned to text."
    - "News articles have a strongly typed data structure."
  artifacts:
    - path: "src/types/news.ts"
      provides: "News data models and Zod schemas"
    - path: "src/lib/news/sentiment.ts"
      provides: "Zero-dependency sentiment analysis"
  key_links:
    - from: "src/lib/news/sentiment.ts"
      to: "Turkish financial keywords"
      via: "Hardcoded dictionary"
---

<objective>
Setup the core data structures and sentiment analysis logic for the news integration.

Purpose: We need a typed representation of news articles and a lightweight way to assign basic sentiment scores before we introduce any expensive LLMs.
Output: TypeScript interfaces, Zod schemas, and a zero-dependency sentiment scoring function.
</objective>

<execution_context>
@C:/Users/duyma/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/duyma/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-high-risk-data-integration-news/04-PATTERNS.md
@.planning/phases/04-high-risk-data-integration-news/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define News Data Models</name>
  <files>src/types/news.ts</files>
  <action>
    Create `src/types/news.ts`. Define the `NewsArticle` interface and a corresponding Zod schema `NewsArticleSchema`.
    The interface must include:
    - `title` (string)
    - `date` (string)
    - `source` (string)
    - `url` (string)
    - `content` (string)
    - `sentiment` (optional, 'positive' | 'negative' | 'neutral')
    Follow the pattern from `04-PATTERNS.md`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>src/types/news.ts exports the interface and Zod schema without type errors.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Dictionary-based Sentiment Analyzer</name>
  <files>src/lib/news/sentiment.ts</files>
  <action>
    Create `src/lib/news/sentiment.ts`. Implement and export a function `analyzeSentiment(text: string): { sentiment: 'positive' | 'negative' | 'neutral', score: number }`.
    Use a zero-dependency approach with hardcoded arrays of positive and negative Turkish financial keywords (e.g., "kâr", "büyüme", "zarar", "düşüş") as researched in `04-RESEARCH.md`. Ensure the text is converted to lowercase ('tr-TR') for matching, and simply add/subtract points based on keyword occurrences.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>src/lib/news/sentiment.ts contains the analyzeSentiment function and correctly compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal logic | Sentiment dictionary acts on string inputs, no external services involved. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | src/types/news.ts | mitigate | Zod schema validation will ensure any external news data strictly matches expected formats before processing. |
</threat_model>

<verification>
- Types compile without error.
- Zod schema perfectly matches the TypeScript interface.
</verification>

<success_criteria>
- The models are ready for the scraper to use.
- Sentiment scoring is available as a reusable utility.
</success_criteria>

<output>
After completion, create `.planning/phases/04-high-risk-data-integration-news/04-01-SUMMARY.md`
</output>
