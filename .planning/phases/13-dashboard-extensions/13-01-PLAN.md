---
phase: 13-dashboard-extensions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260517000000_user_settings.sql
  - src/types/saas.ts
  - src/components/dashboard/SidebarNav.tsx
  - src/app/dashboard/layout.tsx
autonomous: true
requirements:
  - EXT-01
must_haves:
  truths:
    - "User settings table exists in database to store preferences."
    - "Sidebar navigation links correctly reflect active route state."
    - "Sidebar navigation includes valid paths to Data Sources and Settings pages."
  artifacts:
    - path: "supabase/migrations/20260517000000_user_settings.sql"
      provides: "Migration for user_settings table"
    - path: "src/components/dashboard/SidebarNav.tsx"
      provides: "Client-side navigation component with usePathname hook"
  key_links:
    - from: "src/app/dashboard/layout.tsx"
      to: "src/components/dashboard/SidebarNav.tsx"
      via: "replacement of hardcoded <nav> block"
---

<objective>
Database and layout preparations for dashboard extensions. Creates the schema for storing user preferences and updates the Next.js sidebar layout to handle dynamic route states and properly link to the new Data Sources and Settings pages.
Purpose: Necessary foundational steps before building the respective pages.
Output: Supabase migration, SidebarNav component, updated layout.tsx.
</objective>

<execution_context>
@C:/Users/duyma/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/duyma/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/13-dashboard-extensions/PATTERNS.md
@.planning/phases/13-dashboard-extensions/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Supabase Migration and Types</name>
  <files>supabase/migrations/20260517000000_user_settings.sql, src/types/saas.ts</files>
  <action>Create a new Supabase migration file `20260517000000_user_settings.sql` that creates a `user_settings` table. 
The table must have:
- `id` (uuid, PK, default gen_random_uuid())
- `user_id` (varchar, references users(id) ON DELETE CASCADE, unique)
- `theme` (varchar, default 'dark')
- `notifications_enabled` (boolean, default true)
- `external_api_keys` (jsonb, default '{}'::jsonb)
- `created_at`, `updated_at` (timestamptz)

Enable RLS on `user_settings` and create policies so users can only select and update their own row.
Then, update `src/types/saas.ts` to include the `UserSettings` interface matching this table schema.
</action>
  <verify>
    <automated>grep -c "create table.*user_settings" supabase/migrations/20260517000000_user_settings.sql</automated>
  </verify>
  <done>Migration file is created with correct columns and RLS, and types are updated.</done>
</task>

<task type="auto">
  <name>Task 2: SidebarNav Client Component</name>
  <files>src/components/dashboard/SidebarNav.tsx</files>
  <action>Create a Client Component (`'use client'`) named `SidebarNav`. 
It should map over an array of navigation items (Yeni Araştırma -> /dashboard, Raporlarım -> /dashboard/history, Veri Kaynakları -> /dashboard/data-sources, Ayarlar -> /dashboard/settings).
Use `usePathname` from `next/navigation` to determine the `active` state of each link.
Implement the exact Terminal V1.0 HTML/Tailwind classes from the original `layout.tsx` (e.g. `bg-[#22c55e]/08 border-l-2 border-[#facc15] text-white` for active, `text-[#64748b]` for inactive).
Include the material-symbols-outlined icons: 'add', 'description', 'database', 'settings'.
</action>
  <verify>
    <automated>grep -c "usePathname" src/components/dashboard/SidebarNav.tsx</automated>
  </verify>
  <done>Client component created with usePathname handling dynamic styling.</done>
</task>

<task type="auto">
  <name>Task 3: Update layout.tsx</name>
  <files>src/app/dashboard/layout.tsx</files>
  <action>Update `src/app/dashboard/layout.tsx`. Remove the hardcoded `NAV_ITEMS` array and the `<nav>` mapping logic. 
Import `SidebarNav` from `@/components/dashboard/SidebarNav` and render it in place of the old `<nav>`.
Do not alter the rest of the layout (keep the top brand glow, the Data Sources chip below the nav, and the UserButton at the bottom).
</action>
  <verify>
    <automated>grep -c "SidebarNav" src/app/dashboard/layout.tsx</automated>
  </verify>
  <done>layout.tsx uses SidebarNav component.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client → API | RLS policies ensure user can only read/write their own settings. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01-1 | Elevation of Privilege | user_settings DB | mitigate | RLS policy restricts UPDATE/SELECT to matching user_id |
</threat_model>

<verification>
Migration script checks out syntactically. layout.tsx still renders without breaking Server Component boundary.
</verification>

<success_criteria>
`user_settings` table migration is defined, layout properly delegates navigation state to a Client component, enabling new pages.
</success_criteria>

<output>
After completion, create `.planning/phases/13-dashboard-extensions/13-01-SUMMARY.md`
</output>