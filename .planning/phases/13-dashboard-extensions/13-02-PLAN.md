---
phase: 13-dashboard-extensions
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - src/lib/validations/settings.ts
  - src/lib/actions/settings.ts
  - src/components/dashboard/SettingsForm.tsx
  - src/app/dashboard/settings/page.tsx
autonomous: true
requirements:
  - EXT-02
must_haves:
  truths:
    - "User can view their current preferences on the Settings page."
    - "User can save updated preferences and see success/error messages."
    - "Settings form validates input before sending to the server."
  artifacts:
    - path: "src/app/dashboard/settings/page.tsx"
      provides: "Settings server component rendering the form."
    - path: "src/components/dashboard/SettingsForm.tsx"
      provides: "Client-side interactive settings form using Server Actions."
    - path: "src/lib/actions/settings.ts"
      provides: "Server action for upserting user_settings."
  key_links:
    - from: "src/components/dashboard/SettingsForm.tsx"
      to: "src/lib/actions/settings.ts"
      via: "useFormState / React Action"
---

<objective>
Implement the Settings page allowing users to configure their dashboard theme, notification preferences, and any external API keys. Leverages Next.js Server Actions and Zod for robust form handling.
Purpose: Fulfills the user preference requirement of the terminal dashboard.
Output: Working Settings route with server-side validation and DB persistence.
</objective>

<execution_context>
@C:/Users/duyma/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/duyma/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-dashboard-extensions/PATTERNS.md
@.planning/phases/13-dashboard-extensions/RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Validations and Server Actions</name>
  <files>src/lib/validations/settings.ts, src/lib/actions/settings.ts</files>
  <behavior>
    - saveUserSettings action takes formData, validates against settingsSchema.
    - If valid, upserts data in Supabase user_settings for the authenticated user.
    - Revalidates path '/dashboard/settings' and returns { message: "Success" } or error.
  </behavior>
  <action>Create `src/lib/validations/settings.ts` exporting a Zod schema (`settingsSchema`) containing `theme` (string), `notifications_enabled` (boolean, or 'on' string cast to boolean).
Create `src/lib/actions/settings.ts` exporting a `'use server'` function `saveUserSettings(prevState: any, formData: FormData)`.
Use `@supabase/ssr` or `createClient` from `@/lib/supabase/server` to get the current user session. Parse `formData` using Zod. Upsert the row into `user_settings`. Call `revalidatePath('/dashboard/settings')`. Return an object `{ message: string, error?: string }`.
</action>
  <verify>
    <automated>grep -c "'use server'" src/lib/actions/settings.ts</automated>
  </verify>
  <done>Server Action and Zod schema created successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Settings Form Component</name>
  <files>src/components/dashboard/SettingsForm.tsx</files>
  <action>Create a Client Component (`'use client'`) named `SettingsForm`. It should accept `initialData` as props.
Use `useFormState` (from `react-dom`) with `saveUserSettings` action.
Render a form conforming to the Terminal V1.0 aesthetic (inputs with `bg-[#0d0d0d] border border-[#22c55e]/20 text-white font-['JetBrains_Mono']`).
Include a Theme selector (Dark/Light), Notifications toggle/checkbox, and a save button (`border border-[#22c55e]/30 text-[#22c55e] hover:bg-[#22c55e]/10 px-4 py-2`).
Display `state?.message` or `state?.error` below the button in terminal font (`text-[#facc15]` or `text-[#ef4444]`).
</action>
  <verify>
    <automated>grep -c "useFormState" src/components/dashboard/SettingsForm.tsx</automated>
  </verify>
  <done>Settings Form client component built and integrated with server action.</done>
</task>

<task type="auto">
  <name>Task 3: Settings Page Server Component</name>
  <files>src/app/dashboard/settings/page.tsx</files>
  <action>Create the Settings page Server Component at `src/app/dashboard/settings/page.tsx`.
Fetch the current user's settings from Supabase (using `createClient` from `@/lib/supabase/server`).
Render a Bento grid container matching the dashboard layout: `<div className="bg-[#080808] border border-[#22c55e]/12 rounded-xl p-5 flex flex-col gap-4">`.
Include a header `<span className="font-['JetBrains_Mono'] text-[10px] text-[#22c55e]/50 tracking-widest uppercase">// AYARLAR</span>`.
Pass the fetched settings as `initialData` into `<SettingsForm />`.
</action>
  <verify>
    <automated>grep -c "SettingsForm" src/app/dashboard/settings/page.tsx</automated>
  </verify>
  <done>Settings page routes properly and fetches initial data for form hydration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client → Server Action | Input from Settings Form to saveUserSettings. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02-1 | Tampering | saveUserSettings | mitigate | Zod schema validation applied to formData before DB upsert. |
| T-13-02-2 | Spoofing | saveUserSettings | mitigate | Server Action reads userId from auth() session, not from formData payload. |
</threat_model>

<verification>
Ensure the Server Action securely reads the user session and does not trust any user_id passed from the client payload.
</verification>

<success_criteria>
Settings page is accessible at `/dashboard/settings`, visually matches Terminal V1.0 styling, and successfully saves user preferences to the DB.
</success_criteria>

<output>
After completion, create `.planning/phases/13-dashboard-extensions/13-02-SUMMARY.md`
</output>