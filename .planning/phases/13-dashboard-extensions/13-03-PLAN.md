---
phase: 13-dashboard-extensions
plan: 03
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - src/components/dashboard/DataSourceCard.tsx
  - src/app/dashboard/data-sources/page.tsx
autonomous: true
requirements:
  - EXT-03
must_haves:
  truths:
    - "User can view active and inactive data sources on the Data Sources page."
    - "UI utilizes MagicCard and Terminal V1.0 styling for visual feedback."
  artifacts:
    - path: "src/app/dashboard/data-sources/page.tsx"
      provides: "Main data sources routing page."
    - path: "src/components/dashboard/DataSourceCard.tsx"
      provides: "Client component wrapping MagicCard for individual source display."
  key_links:
    - from: "src/app/dashboard/data-sources/page.tsx"
      to: "src/components/dashboard/DataSourceCard.tsx"
      via: "array mapping of available sources"
---

<objective>
Implement the Data Sources page using MagicUI components to visualize connection statuses (KAP, TCMB, Bloomberg, Reuters) conforming to the Terminal V1.0 aesthetic.
Purpose: Provides transparency to the user regarding which scraping and API modules are currently operational.
Output: Working Data Sources route displaying glowing status cards.
</objective>

<execution_context>
@C:/Users/duyma/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/duyma/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-dashboard-extensions/PATTERNS.md
@.planning/phases/13-dashboard-extensions/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: DataSourceCard Component</name>
  <files>src/components/dashboard/DataSourceCard.tsx</files>
  <action>Create a Client Component named `DataSourceCard`. 
It should accept props: `name` (string), `status` ('active' | 'inactive' | 'error'), `description` (string).
Import `MagicCard` from `@/components/ui/MagicCard`. Wrap the component content in `MagicCard` with `className="bg-[#080808] border border-[#22c55e]/12 p-4 flex flex-col gap-3"` and `gradientColor="rgba(34,197,94,0.1)"`.
Render the name using terminal styling (e.g. `font-['JetBrains_Mono'] text-sm`).
Render the status using the glowing ping indicator pattern:
```tsx
<span className="relative flex h-2 w-2">
  <span className={`status-ping absolute inline-flex h-full w-full rounded-full opacity-60 ${status === 'active' ? 'bg-[#22c55e]' : status === 'error' ? 'bg-[#ef4444]' : 'bg-[#64748b]'}`} />
  <span className={`relative inline-flex rounded-full h-2 w-2 ${status === 'active' ? 'bg-[#22c55e]' : status === 'error' ? 'bg-[#ef4444]' : 'bg-[#64748b]'}`} />
</span>
```
Include `description` as muted text `text-[#64748b] text-xs`.
</action>
  <verify>
    <automated>grep -c "MagicCard" src/components/dashboard/DataSourceCard.tsx</automated>
  </verify>
  <done>Client component created with glowing effects and terminal styling.</done>
</task>

<task type="auto">
  <name>Task 2: Data Sources Page</name>
  <files>src/app/dashboard/data-sources/page.tsx</files>
  <action>Create the Data Sources Server Component page.
Define an array of sources with mock operational statuses (KAP: active, TCMB: active, Bloomberg: active, Reuters: active).
Render a header `<h2 className="font-['JetBrains_Mono'] text-lg text-[#22c55e]/80 uppercase">// VERİ KAYNAKLARI_</h2>`.
Map over the sources and render `<DataSourceCard />` for each within a grid layout (`grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4`).
Apply Magic UI fade-in `className="blur-fade blur-fade-d1"`.
</action>
  <verify>
    <automated>grep -c "DataSourceCard" src/app/dashboard/data-sources/page.tsx</automated>
  </verify>
  <done>Data Sources page renders a grid of MagicCard wrapped connection statuses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client | UI elements strictly for display purposes based on server-provided statuses. No sensitive keys exposed. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03-1 | Information Disclosure | Data Sources Page | accept | Exposing general service status (active/down) poses minimal risk and is required for UX transparency. |
</threat_model>

<verification>
Ensure MagicCard renders correctly on client side without hydration mismatches.
</verification>

<success_criteria>
Data Sources page is accessible at `/dashboard/data-sources` and visually displays sources using Terminal V1.0 styling and MagicUI effects.
</success_criteria>

<output>
After completion, create `.planning/phases/13-dashboard-extensions/13-03-SUMMARY.md`
</output>