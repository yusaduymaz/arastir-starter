---
phase: "06"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: ["supabase/migrations/20260513000003_saas_tables.sql", "src/types/saas.ts"]
autonomous: true
requirements: ["SAAS-01", "SAAS-03"]

must_haves:
  truths:
    - "A `users` table exists in Supabase to track tiers and tokens."
    - "TypeScript interfaces accurately reflect the database schema for users."
---

<objective>
Setup the database schema for the SaaS model, including the `users` table with tiers and tokens.
</objective>

<context>
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Supabase Migration for Users</name>
  <files>supabase/migrations/20260513000003_saas_tables.sql</files>
  <action>
    Create a new migration file.
    Create a `users` table with the following columns:
    - `id` (TEXT PRIMARY KEY) - This will store the Clerk User ID.
    - `email` (TEXT NOT NULL)
    - `tier` (TEXT NOT NULL DEFAULT 'free') - Enums or check constraint: 'free', 'starter', 'growth', 'agency'.
    - `tokens_balance` (INTEGER NOT NULL DEFAULT 5)
    - `created_at` (TIMESTAMP WITH TIME ZONE DEFAULT NOW())
    
    Add RLS policies. The service role must be able to insert/update, and users can read their own row:
    `CREATE POLICY "Allow individual read access" ON users FOR SELECT USING (auth.jwt()->>'sub' = id);`
    Enable RLS on the table.
  </action>
</task>

<task type="auto">
  <name>Task 2: Create TypeScript Models</name>
  <files>src/types/saas.ts</files>
  <action>
    Create `src/types/saas.ts` defining the `UserProfile` interface matching the database schema.
    Export a `Tier` type: `'free' | 'starter' | 'growth' | 'agency'`.
  </action>
</task>

</tasks>

<success_criteria>
- The migration file is created and syntactically correct.
- TypeScript interfaces exist.
</success_criteria>
