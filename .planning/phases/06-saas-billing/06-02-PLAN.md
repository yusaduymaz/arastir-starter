---
phase: "06"
plan: "02"
type: execute
wave: 2
depends_on: ["06-01"]
files_modified: ["src/app/api/webhooks/clerk/route.ts"]
autonomous: true
requirements: ["SAAS-01"]

must_haves:
  truths:
    - "A webhook endpoint securely receives user.created events from Clerk."
    - "New users are inserted into the Supabase `users` table with default tokens."
---

<objective>
Implement Clerk Webhooks to automatically sync new users to the Supabase database.
</objective>

<context>
@.planning/phases/06-saas-billing/06-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Svix for Webhook Verification</name>
  <files>package.json</files>
  <action>
    Run `npm install svix` to handle secure webhook verification from Clerk.
  </action>
</task>

<task type="auto">
  <name>Task 2: Implement Webhook Route</name>
  <files>src/app/api/webhooks/clerk/route.ts</files>
  <action>
    Create the webhook route handling `POST` requests.
    Verify the webhook signature using `svix` and `process.env.CLERK_WEBHOOK_SECRET`.
    If the event is `user.created`:
    Extract `id` and `email_addresses[0].email_address` from the payload.
    Use the Supabase Service Role client to insert the user into the `users` table with `tier: 'free'` and `tokens_balance: 5`.
    Return a 200 OK response.
  </action>
</task>

</tasks>

<success_criteria>
- Webhook endpoint is secure and properly processes Clerk events.
</success_criteria>
