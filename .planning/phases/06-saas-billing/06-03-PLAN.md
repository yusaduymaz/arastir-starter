---
phase: "06"
plan: "03"
type: execute
wave: 3
depends_on: ["06-01"]
files_modified: ["src/app/api/research/route.ts", "src/components/dashboard/TopSearchBar.tsx"]
autonomous: true
requirements: ["SAAS-02", "SAAS-03"]

must_haves:
  truths:
    - "Research requests deduct tokens from the user's balance."
    - "Users with 0 tokens are blocked from submitting queries."
    - "The UI displays token balance dynamically."
---

<objective>
Enforce token consumption rules on the backend and display usage limits on the frontend dashboard.
</objective>

<context>
@.planning/phases/06-saas-billing/06-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce Tokens in Research API</name>
  <files>src/app/api/research/route.ts</files>
  <action>
    Modify `POST` handler in `src/app/api/research/route.ts`.
    Before creating a `research_history` record, query the `users` table for the `userId`.
    Check if `tokens_balance > 0`. If not, return a 403 Forbidden with a message "Yetersiz token. Lütfen planınızı yükseltin."
    If tokens exist, proceed with the research pipeline.
    Deduct 1 token from the user's balance in Supabase.
  </action>
</task>

<task type="auto">
  <name>Task 2: Create Token Display Component</name>
  <files>src/components/dashboard/TokenDisplay.tsx, src/app/dashboard/layout.tsx</files>
  <action>
    Create a server or client component that fetches the current user's `tokens_balance` and `tier` from the `users` table.
    Display this beautifully in the sidebar or top header (e.g., "Kalan Token: 4 (Free Plan)").
    Add a dummy "Planı Yükselt" (Upgrade Plan) button that will later link to Stripe.
  </action>
</task>

<task type="auto">
  <name>Task 3: Handle Insufficient Tokens UI</name>
  <files>src/components/dashboard/TopSearchBar.tsx</files>
  <action>
    Update error handling in the `TopSearchBar` to show a specific alert or modal if the API returns the 403 insufficient tokens error.
  </action>
</task>

</tasks>

<success_criteria>
- Token deduction logic works flawlessly.
- Users cannot bypass the token limit.
- The Dashboard UI reflects limits clearly.
</success_criteria>
