---
phase: 03-high-risk-data-integration-kap
plan: 03
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - "src/agents/search-agent.ts"
autonomous: true
requirements:
  - AGENT-02

must_haves:
  truths:
    - "A search agent orchestrates the scraper via CLI arguments."
    - "The extracted data is validated against the defined schema before saving."
    - "The agent saves its findings to a structured JSON file in the research directory."
  artifacts:
    - path: "src/agents/search-agent.ts"
      provides: "CLI entrypoint for KAP scraping"
  key_links:
    - from: "src/agents/search-agent.ts"
      to: "src/lib/kap/client.ts"
      via: "import fetchDisclosures"
      pattern: "import.*fetchDisclosures"
    - from: "src/agents/search-agent.ts"
      to: "research/"
      via: "fs.writeFileSync"
      pattern: "writeFileSync.*research/.*json"
---

<objective>
Create the 'search' agent and integrate it with the KAP scraper module.

Purpose: To fulfill requirement AGENT-02 by wrapping the scraping logic in a command-line agent architecture. It coordinates the scrape, validates the data, and saves the output for downstream synthesis.
Output: A `search-agent.ts` executable file.
</objective>

<execution_context>
@C:/Users/duyma/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/duyma/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/lib/kap/client.ts
@src/types/kap.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the Search Agent CLI</name>
  <files>src/agents/search-agent.ts</files>
  <action>
  Create `src/agents/search-agent.ts`.
  As defined in `03-PATTERNS.md`, the search agent mirrors the path handling and execution encapsulation from `src/agents/run-writer.ts`, combined with CLI argument handling.
  1. Import `fs` and `path`.
  2. Import `fetchDisclosures` from `../lib/kap/client`.
  3. Import `KAPDisclosureSchema` from `../types/kap`.
  4. Define an async `runSearchAgent(topic: string)` function.
  5. Parse `process.argv[2]` for the topic/ticker. If missing, log an error and exit.
  6. Call `fetchDisclosures(topic)`.
  7. Validate the returned array using `z.array(KAPDisclosureSchema).parse(data)` to ensure data integrity.
  8. Save the validated JSON to `research/{timestamp}-{topic}-kap.json`. Ensure the `research/` directory exists.
  9. Add appropriate `try/catch` and console logging for success and error states.
  </action>
  <verify>
    <automated>grep -v '^#' src/agents/search-agent.ts | grep -c "fetchDisclosures"</automated>
  </verify>
  <done>Agent parses CLI argument, fetches data, validates it, and saves to file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent -> File System | Agent writes untrusted data to the local file system. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | Agent Execution | Mitigate | The agent validates the returned data from the scraper using `KAPDisclosureSchema` (Zod) before saving, ensuring malformed payloads are rejected. |
| T-03-05 | Information Disclosure | File System | Accept | The saved JSON files will reside in the `research/` directory locally. Assuming standard local permissions. |
</threat_model>

<verification>
- `npx ts-node src/agents/search-agent.ts THYAO` runs successfully.
- A new file `research/{timestamp}-THYAO-kap.json` is created with valid JSON matching the schema.
</verification>

<success_criteria>
- The search agent is complete and meets the `AGENT-02` requirement.
</success_criteria>

<output>
After completion, create `.planning/phases/03-high-risk-data-integration-kap/03-03-SUMMARY.md`
</output>