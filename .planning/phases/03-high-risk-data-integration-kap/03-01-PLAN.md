---
phase: 03-high-risk-data-integration-kap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "package.json"
  - "src/types/kap.ts"
  - "src/lib/kap/client.ts"
autonomous: true
requirements:
  - SCRAPE-01

must_haves:
  truths:
    - "Stealth Puppeteer dependencies are installed to avoid bot detection."
    - "KAP data models are defined using strict TypeScript interfaces."
    - "A basic client skeleton can initialize a stealth browser and open kap.org.tr."
  artifacts:
    - path: "package.json"
      provides: "puppeteer-extra and stealth plugin dependencies"
    - path: "src/types/kap.ts"
      provides: "KAPDisclosure interface"
      exports: ["KAPDisclosure"]
    - path: "src/lib/kap/client.ts"
      provides: "Stealth browser initialization wrapper"
      exports: ["fetchDisclosures"]
  key_links:
    - from: "src/lib/kap/client.ts"
      to: "src/types/kap.ts"
      via: "import KAPDisclosure type"
      pattern: "import.*KAPDisclosure"
---

<objective>
Setup the KAP Data Models and Stealth Scraper Foundation.

Purpose: To de-risk the scraping of KAP which employs strict bot detection by setting up `puppeteer-extra` with the stealth plugin, and defining the domain models for disclosures.
Output: Dependencies installed, models defined, and an initial scraper skeleton.
</objective>

<execution_context>
@C:/Users/duyma/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/duyma/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-high-risk-data-integration-kap/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Stealth Puppeteer Dependencies</name>
  <files>package.json</files>
  <action>
  Install `puppeteer`, `puppeteer-extra`, `puppeteer-extra-plugin-stealth`, and `zod` as dependencies.
  Do not use `npm i -D` for these, as they are runtime dependencies for the backend agent.
  Use quiet flags for installation (e.g. `npm install puppeteer puppeteer-extra puppeteer-extra-plugin-stealth zod --silent`).
  </action>
  <verify>
    <automated>grep -c '"puppeteer-extra"' package.json</automated>
  </verify>
  <done>Dependencies added to package.json.</done>
</task>

<task type="auto">
  <name>Task 2: Define KAP Data Models</name>
  <files>src/types/kap.ts</files>
  <action>
  Create `src/types/kap.ts`.
  As defined in `03-PATTERNS.md`, new models should export simple interfaces similar to `src/types/research.ts`.
  Define and export a TypeScript interface `KAPDisclosure` with:
  - `title` (string)
  - `date` (string)
  - `company` (string)
  - `url` (string)
  - `content` (string)
  Also, export a Zod schema `KAPDisclosureSchema` matching this interface to validate runtime data.
  </action>
  <verify>
    <automated>grep -v '^#' src/types/kap.ts | grep -c "interface KAPDisclosure"</automated>
  </verify>
  <done>Model file exports KAPDisclosure interface and Zod schema.</done>
</task>

<task type="auto">
  <name>Task 3: Create Stealth Scraper Skeleton</name>
  <files>src/lib/kap/client.ts</files>
  <action>
  Create `src/lib/kap/client.ts`.
  As per `03-PATTERNS.md`, the client should follow the request-response layout of `src/lib/tcmb/client.ts`, exporting an async function with robust error catching and structured logging.
  Import `puppeteer` from `puppeteer-extra`.
  Import `StealthPlugin` from `puppeteer-extra-plugin-stealth`.
  Import `KAPDisclosure` from `../../types/kap`.
  
  Initialize the stealth plugin: `puppeteer.use(StealthPlugin());`
  
  Export an async function `fetchDisclosures(ticker: string): Promise<KAPDisclosure[]>` that:
  1. Launches stealth browser (`headless: true`, args: `['--no-sandbox', '--disable-setuid-sandbox']`).
  2. Opens a new page.
  3. Navigates to `https://www.kap.org.tr/tr/bildirim-sorgu`.
  4. Returns an empty array `[]` for now.
  5. Closes the browser in a `finally` block to ensure no orphaned processes.
  </action>
  <verify>
    <automated>grep -v '^#' src/lib/kap/client.ts | grep -c "puppeteer.use(StealthPlugin())"</automated>
  </verify>
  <done>fetchDisclosures function sets up stealth browser and navigates to target without errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scraper -> KAP Platform | Retrieving data from kap.org.tr bypassing simple bot detection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Denial of Service | KAP Client | Mitigate | Agent must always clean up browser process in a `finally` block to avoid memory exhaustion from orphaned puppeteer instances. |
</threat_model>

<verification>
- `npm ls puppeteer-extra` succeeds.
- `src/lib/kap/client.ts` compiles without type errors.
</verification>

<success_criteria>
- Project is configured for stealth scraping with necessary packages.
- Domain models are established.
</success_criteria>

<output>
After completion, create `.planning/phases/03-high-risk-data-integration-kap/03-01-SUMMARY.md`
</output>