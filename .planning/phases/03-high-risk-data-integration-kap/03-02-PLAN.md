---
phase: 03-high-risk-data-integration-kap
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - "src/lib/kap/client.ts"
  - "src/lib/kap/test.ts"
autonomous: true
requirements:
  - SCRAPE-01

must_haves:
  truths:
    - "Scraper can navigate KAP, bypass modals, and search for a ticker."
    - "Scraper extracts the top 5 disclosures' metadata and full content."
    - "Test script can successfully retrieve THYAO disclosures locally."
  artifacts:
    - path: "src/lib/kap/client.ts"
      provides: "Full DOM extraction logic"
      exports: ["fetchDisclosures"]
    - path: "src/lib/kap/test.ts"
      provides: "Local test script for the scraper"
  key_links:
    - from: "src/lib/kap/test.ts"
      to: "src/lib/kap/client.ts"
      via: "import fetchDisclosures"
      pattern: "import.*fetchDisclosures"
---

<objective>
Implement the KAP DOM extraction logic.

Purpose: To extract the actual disclosure data from the React/Next.js rendered page using DOM selectors, handling any modals or loading states to yield the 5 most recent disclosures.
Output: A fully functional `fetchDisclosures` scraper and a test script to verify it.
</objective>

<execution_context>
@C:/Users/duyma/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/duyma/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-high-risk-data-integration-kap/03-RESEARCH.md
@src/types/kap.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Scraping and DOM Extraction</name>
  <files>src/lib/kap/client.ts</files>
  <action>
  Update `fetchDisclosures(ticker: string)` in `src/lib/kap/client.ts` to implement the full flow:
  1. Set a realistic User-Agent (e.g., standard Chrome on Windows).
  2. Navigate to `https://www.kap.org.tr/tr/bildirim-sorgu`.
  3. Wait for the page to load. Dismiss any cookie banners or modals if they exist (use `page.$` to check and `page.click` to close, catching errors if they don't appear).
  4. Find the company search input, type the `ticker`, and trigger the search (you may need to click an autocomplete dropdown item or a search button).
  5. Wait for the loading spinner (`img[src*="gifload.gif"]` or similar) to disappear (use `waitForFunction` or wait for the results container).
  6. Query the results container (e.g., `.notifications.v-row` -> `.v-col-12`) and slice to the top 5 results.
  7. For each result, extract the title, date, company name, and the detail URL (`href`).
  8. For each detail URL, open a new page (or navigate), wait for content, extract the main text (`content`), and close the page.
  9. Assemble and return an array of `KAPDisclosure` objects.
  Wrap operations in `try/catch` and ensure the main browser instance is closed in a `finally` block.
  </action>
  <verify>
    <automated>grep -c "page.\$\$eval" src/lib/kap/client.ts</automated>
  </verify>
  <done>fetchDisclosures function is updated to interact with the page and parse DOM elements.</done>
</task>

<task type="auto">
  <name>Task 2: Create Scraper Test Script</name>
  <files>src/lib/kap/test.ts</files>
  <action>
  Create `src/lib/kap/test.ts`.
  As defined in `03-PATTERNS.md`, this test script should borrow the standalone asynchronous IIFE or `main()` script structure seen in `src/agents/test-tcmb-agent.ts`.
  Import `fetchDisclosures` from `./client`.
  Create a self-executing async function that calls `await fetchDisclosures('THYAO')`.
  Log the resulting JSON array to the console (`console.log(JSON.stringify(data, null, 2))`).
  Catch and log any errors, exiting the process with code 1 if failed.
  </action>
  <verify>
    <automated>grep -c "fetchDisclosures('THYAO')" src/lib/kap/test.ts</automated>
  </verify>
  <done>Test script exists and invokes the scraper.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scraper -> DOM | Untrusted HTML content from KAP could contain unexpected structures causing extraction to fail. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Information Disclosure | Scraper Logic | Mitigate | Use standard User-Agent and mimic user interaction (typing delays) to avoid revealing bot identity. |
| T-03-03 | Denial of Service | KAP Extraction | Mitigate | Implement strict timeouts on `waitForSelector` to prevent the scraper from hanging indefinitely on DOM changes. |
</threat_model>

<verification>
- `npx ts-node src/lib/kap/test.ts` executes and returns an array of up to 5 disclosures.
</verification>

<success_criteria>
- Scraper successfully retrieves and parses data from KAP.
- Results match the expected KAPDisclosure schema.
</success_criteria>

<output>
After completion, create `.planning/phases/03-high-risk-data-integration-kap/03-02-SUMMARY.md`
</output>