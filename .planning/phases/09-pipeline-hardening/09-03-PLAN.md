---
phase: "09"
plan: "03"
type: execute
wave: 2
depends_on: ["09-01", "09-02"]
files_modified: ["src/agents/macro-agent.ts (new)", "src/app/api/research/route.ts", "src/lib/tuik/client.ts", "src/agents/analyst-agent.ts"]
autonomous: true
requirements: ["DATA-03", "AI-03"]

must_haves:
  truths:
    - "A new macro-agent.ts fetches USD/TRY rate, CPI (TÜFE), and policy rate from TCMB EVDS for the last 12 months and writes <ts>-macro.json to research/."
    - "TÜİK client either successfully fetches statistical themes from a working endpoint OR falls back to a versioned static dataset that is not silently masked as live."
    - "Analyst prompt receives the macro data block and is instructed to cite specific values (USD rate, CPI trend, rate direction) in the Risks/Opportunities sections."
---

<objective>
Wire the previously unused TCMB and TÜİK clients into the pipeline. The current pipeline has rich macroeconomic data sitting unused — the analyst gets zero macro context, which is why its synthesis is generic. Adding macro data turns "company in isolation" reports into "company in current macro climate" reports.
</objective>

<context>
TCMB EVDS client (src/lib/tcmb/client.ts) is implemented and working (TCMB_EVDS_API_KEY is set). It exposes getMacroEconomicData(start, end, period) which returns USD/TRY, CPI, and policy rate. None of this data flows into any agent. TÜİK client (src/lib/tuik/client.ts) currently falls back to a hardcoded theme list because the new portal API returns HTML; we accept the fallback but make it explicit and versioned.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create macro-agent.ts</name>
  <files>src/agents/macro-agent.ts</files>
  <read_first>src/lib/tcmb/client.ts, src/agents/market-agent.ts</read_first>
  <action>
    Create src/agents/macro-agent.ts following the exact structure of market-agent.ts:
    - Export `runMacroAgent(_ticker: string)` (ticker accepted for symmetry but unused).
    - Internally compute the date range (today minus 365 days → today, DD-MM-YYYY format).
    - Call `getMacroEconomicData(start, end, 'monthly')`.
    - Write result to `<timestamp>-macro.json` in process.cwd()/research/. Filename does NOT include ticker because macro data is shared across all queries.
    - ESM-safe CLI wrapper using `process.argv[1].includes('macro-agent')`.
  </action>
  <acceptance_criteria>
    - File src/agents/macro-agent.ts exists.
    - `npx tsx src/agents/macro-agent.ts THYAO` exits 0 and writes a `*-macro.json` file containing at least 6 monthly entries with non-null TP_DK_USD_A.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add macro to parallel agent execution</name>
  <files>src/app/api/research/route.ts</files>
  <read_first>src/app/api/research/route.ts</read_first>
  <action>
    In executeResearchPipeline, add a fourth Promise.allSettled entry for macro-agent. Add a corresponding "started" log entry. Macro is degraded-allowed (failure does not abort pipeline). Read the latest `*-macro.json` (no ticker filter — pick the newest file matching `/^\d+-macro\.json$/`) and pass to runAnalystAgent.
  </action>
  <acceptance_criteria>
    - route.ts contains a macroAgentCmd variable and a fourth Promise.allSettled entry.
    - agent_logs after a run contains entries for both macro started and completed/failed.
    - When macro succeeds, analyst is called with a non-null macroData argument.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add macroData parameter to analyst-agent</name>
  <files>src/agents/analyst-agent.ts</files>
  <read_first>src/agents/analyst-agent.ts</read_first>
  <action>
    Extend AnalystInsights signature: `runAnalystAgent(ticker, kapData, newsData, marketData, macroData)`. Inject a "Makroekonomik Bağlam" section into the prompt with the last 3 monthly data points (USD, TÜFE, policy rate). Instruct the model to cite specific values when discussing macro-driven risks (e.g., "USD/TRY {value} seviyesinde — döviz açığı olan şirketler için …"). Update the mock fallback to also include a one-line macro reference if macroData is provided.
  </action>
  <acceptance_criteria>
    - analyst-agent.ts signature accepts macroData (typed or `any` if no type defined).
    - When macroData is present, the prompt contains the string "Makroekonomik Bağlam" and at least one numeric value from the data.
    - With ANTHROPIC_API_KEY set, output mentions USD/TRY or TÜFE/CPI in risks or opportunities.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Version the TÜİK fallback explicitly</name>
  <files>src/lib/tuik/client.ts</files>
  <read_first>src/lib/tuik/client.ts</read_first>
  <action>
    In tuik/client.ts, refactor fallbackThemes to a top-level exported constant `FALLBACK_TUIK_THEMES_2025` with a JSDoc note that it was last synced on a specific date. Change the warn log to include the literal string "Using static TUIK fallback (synced 2025)". This makes "live vs static" obvious in logs.
  </action>
  <acceptance_criteria>
    - tuik/client.ts exports FALLBACK_TUIK_THEMES_2025 with a JSDoc @lastSynced tag.
    - Logs contain the phrase "static TUIK fallback" not just "Using fallback themes".
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. Submit THYAO query — check agent_logs contains a macro entry and the analyst prompt (via console) includes a "Makroekonomik Bağlam" section.
2. With ANTHROPIC_API_KEY set, generated report.pdf risks section references USD/TRY or TÜFE.
3. Set CURRENTS_API_KEY to empty and TCMB_EVDS_API_KEY to a wrong value — pipeline completes with macro=null, news=[]; only KAP-driven report.
</verification>
</content>
</invoke>