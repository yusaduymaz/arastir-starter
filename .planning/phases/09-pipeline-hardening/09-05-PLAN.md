---
phase: "09"
plan: "05"
type: execute
wave: 3
depends_on: ["09-03", "09-04"]
files_modified: ["src/agents/analyst-agent.ts", "src/lib/pdf/generator.ts", "src/lib/pptx/generator.ts", ".env.example"]
autonomous: true
requirements: ["AI-04", "REPORT-03"]

must_haves:
  truths:
    - "Analyst agent uses claude-sonnet-4-6 (not claude-3-haiku-20240307) when ANTHROPIC_API_KEY is set."
    - "JSON parsing of the model response uses a tool-use / structured-output strategy that does not break when the model emits <thought>...</thought> blocks or pre-JSON prose."
    - "Generated PDF and PPTX include a dedicated 'Makroekonomik Bağlam' section sourced from macro data."
---

<objective>
Upgrade the AI synthesis from a Haiku-era prompt with fragile JSON extraction to a Sonnet-4.6 prompt with structured tool-use output. Add macro context as a first-class section in the rendered reports. The synthesis is the central value proposition of the product — its quality determines whether a user returns.
</objective>

<context>
Current analyst-agent uses `claude-3-haiku-20240307` (released March 2024, now outclassed) and parses output with `contentText.indexOf('{')` → `JSON.parse` of a substring. This breaks whenever the model emits a <thought> block (which the prompt itself requests) before the JSON. The mock fallback returns three identical canned strings. Plan 09-03 added macro data to the prompt; this plan extends that to the rendered report and upgrades the model.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade model to claude-sonnet-4-6</name>
  <files>src/agents/analyst-agent.ts</files>
  <read_first>src/agents/analyst-agent.ts</read_first>
  <action>
    Change `model: "claude-3-haiku-20240307"` to `model: "claude-sonnet-4-6"`. Increase max_tokens to 4000 to accommodate longer Sonnet outputs. Update the system prompt to instruct the model to act as a senior BIST equity analyst with macroeconomic awareness.
  </action>
  <acceptance_criteria>
    - analyst-agent.ts contains the literal string `claude-sonnet-4-6`.
    - max_tokens is at least 4000.
    - System prompt mentions "BIST" or "Turkish equities".
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Replace indexOf-JSON parsing with tool-use</name>
  <files>src/agents/analyst-agent.ts</files>
  <read_first>src/agents/analyst-agent.ts</read_first>
  <action>
    Replace the current `messages.create` call with one that uses a `tools` parameter and `tool_choice: { type: 'tool', name: 'submit_analysis' }`. The tool schema has properties: executiveSummary, risks, opportunities, macroContext (all strings). Read the structured output from `msg.content[0].input` (tool-use block) instead of parsing free-form text. Remove the `indexOf('{')` substring extraction.
  </action>
  <acceptance_criteria>
    - analyst-agent.ts does NOT contain `contentText.indexOf('{')` or `JSON.parse(contentText.substring(`.
    - The Anthropic call includes a `tools` array with a `submit_analysis` tool.
    - Returned AnalystInsights has all four fields populated when the API call succeeds.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add macroContext to AnalystInsights + report</name>
  <files>src/agents/analyst-agent.ts, src/lib/pdf/generator.ts, src/lib/pptx/generator.ts, src/types/research.ts</files>
  <read_first>src/lib/pdf/generator.ts, src/lib/pptx/generator.ts, src/types/research.ts</read_first>
  <action>
    Extend AnalystInsights interface and ReportData to include `macroContext: string`. In the PDF generator add a new section "Makroekonomik Bağlam" between Executive Summary and Risks. In the PPTX generator add a dedicated macro-context slide between the title and the risks slide. Update the route.ts reportData construction to pass macroContext through.
  </action>
  <acceptance_criteria>
    - Generated PDF contains a "Makroekonomik Bağlam" heading.
    - Generated PPTX has a slide titled "Makroekonomik Bağlam".
    - When macroContext is empty (e.g., macro agent failed), the section/slide is omitted (no empty heading rendered).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Improve degraded-mode (mock) fallback</name>
  <files>src/agents/analyst-agent.ts</files>
  <read_first>src/agents/analyst-agent.ts</read_first>
  <action>
    When ANTHROPIC_API_KEY is missing, log a single clear warning line ("Analyst running in MOCK mode — set ANTHROPIC_API_KEY for real synthesis") and return a mock that incorporates whatever data IS available (kap.length, news.length, marketData.quote.price, macroData last USD value) rather than generic canned text. The mock must clearly self-label as a mock in the output text so it never poses as real analysis in downstream PDFs.
  </action>
  <acceptance_criteria>
    - Without ANTHROPIC_API_KEY, the executiveSummary contains the literal string "[MOCK]" or "[Demo]" prefix.
    - Without ANTHROPIC_API_KEY, the executiveSummary references the actual ticker, KAP count, and USD value from inputs.
    - Logs print exactly one warning line per request (not on every internal call).
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. With ANTHROPIC_API_KEY set, submit THYAO query. Generated report.pdf has a "Makroekonomik Bağlam" section with non-trivial content (≥80 words) that references at least one numeric macro value.
2. With ANTHROPIC_API_KEY unset, the same query completes; report.pdf shows "[MOCK]" prefix and the dashboard log clearly shows "Analyst running in MOCK mode".
3. Force the model to emit a `<thought>` preamble — JSON parsing must still succeed (tool-use bypasses the text parsing path entirely).
4. Run twice; second run uses cached market data (from 09-04) and freshly-fetched KAP — no model-quality regression.
</verification>
</content>
</invoke>