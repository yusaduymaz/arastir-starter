---
phase: "09"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified: ["src/app/api/research/route.ts", "src/components/dashboard/ActivePipeline.tsx", "src/types/research.ts"]
autonomous: true
requirements: ["HARDEN-03", "UI-06"]

must_haves:
  truths:
    - "When an agent fails, the orchestrator logs WHICH agent + a stderr tail (last 300 chars) to agent_logs.details — never a generic 'files not found'."
    - "KAP agent is treated as critical (its failure aborts the pipeline). News and Market are degraded-allowed (pipeline continues with empty arrays / null marketData)."
    - "The ActivePipeline component renders agent_logs.details for failed entries so the user can read WHY without leaving the dashboard."
---

<objective>
Replace the generic "files not found" error with per-agent failure surfaces. Make criticality explicit: KAP is required (BIST disclosure data is the analysis core), News and Market are optional (degraded mode allowed).
</objective>

<context>
Currently the orchestrator's Promise.allSettled block logs per-agent failure but the file-existence check throws a generic error that masks WHICH agent broke. Users see "Ajanlar başarılı görünüyor ancak araştırma çıktı dosyaları bulunamadı." — useless for triage. This plan codifies the per-agent surface introduced in the hotfix and extends it to the UI.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collect per-agent failures before file read</name>
  <files>src/app/api/research/route.ts</files>
  <read_first>src/app/api/research/route.ts</read_first>
  <action>
    After the Promise.allSettled block, build a string[] of agentFailures by inspecting each result's `.status === 'rejected'` and extracting `(reason as any)?.stderr` (last 300 chars) or the reason itself. Format each entry as "KAP: <message>", "News: <message>", "Market: <message>". The hotfix already added this; verify it is present and refactor to a helper function `collectAgentFailures(results: PromiseSettledResult[])` for clarity.
  </action>
  <acceptance_criteria>
    - route.ts contains a function or inline block that produces a string[] of failures keyed by agent name.
    - Each entry contains the agent label (KAP|News|Market) and a non-empty error excerpt.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Tier agents by criticality</name>
  <files>src/app/api/research/route.ts</files>
  <read_first>src/app/api/research/route.ts</read_first>
  <action>
    Replace the legacy `if (kapFiles.length === 0 || newsFiles.length === 0)` block with:
    - If `kapFiles.length === 0` → throw `KAP bildirimleri çekilemedi (<ticker>). Agent hataları: <joined failures>`.
    - If `newsFiles.length === 0` → set `newsData = []`, log a degraded-mode entry to agent_logs.
    - If `marketFiles.length === 0` → set `marketData = null`, log a degraded-mode entry.
    Final orchestrator log must list any degraded agents.
  </action>
  <acceptance_criteria>
    - route.ts never throws "Ajanlar başarılı görünüyor" string.
    - When News agent is forced to fail (e.g., wrong CURRENTS_API_KEY), pipeline completes with newsData=[] and a degraded-mode entry in agent_logs.
    - When KAP agent fails, pipeline aborts with an error message containing "KAP" and the stderr tail.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Surface details in ActivePipeline UI</name>
  <files>src/components/dashboard/ActivePipeline.tsx</files>
  <read_first>src/components/dashboard/ActivePipeline.tsx, src/types/research.ts</read_first>
  <action>
    In the agent activity log render loop, when `log.status === 'failed'` and `log.details` is non-empty, render the details on a second line below the message — same font, dimmer color, max 2 lines with text overflow ellipsis. Add a small expand/collapse caret if details exceed 2 lines.
  </action>
  <acceptance_criteria>
    - ActivePipeline.tsx renders `log.details` for failed log entries.
    - The details line uses font-mono and a muted color (e.g., #64748b).
    - Visible in browser when a failed entry has non-empty details.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Add error_message surface card</name>
  <files>src/components/dashboard/ActivePipeline.tsx</files>
  <read_first>src/components/dashboard/ActivePipeline.tsx</read_first>
  <action>
    When `task.status === 'failed'` and `task.error_message` is set, render a red-bordered card at the top of the ActivePipeline component showing the error_message. Include a "Yeniden Dene" button that POSTs to /api/research with the same query.
  </action>
  <acceptance_criteria>
    - Failed task displays its error_message above the agent log.
    - "Yeniden Dene" button is functional (triggers a new research request with the same query).
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. Submit a query with CURRENTS_API_KEY temporarily unset in .env.local — pipeline should still complete (degraded mode), final report shows "Haber sayısı: 0".
2. Submit a query for an invalid ticker (e.g., "ZZZZZ") — KAP agent fails, pipeline aborts, dashboard shows a red error card with text mentioning "KAP" and Puppeteer error excerpt.
3. Submit "THYAO" with all keys present — completes 0→100, no failed entries.
</verification>
</content>
</invoke>