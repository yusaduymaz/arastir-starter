---
phase: "07"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: ["src/lib/kap/client.ts", "src/lib/news/bloomberg.ts", "src/lib/news/ekonomim.ts"]
autonomous: true
requirements: ["REFACTOR-01"]

must_haves:
  truths:
    - "Scrapers handle timeouts and dynamic DOM changes robustly."
    - "Scrapers return clear error messages instead of failing silently or returning empty arrays."
---

<objective>
Refactor the web scrapers to ensure reliability and handle edge cases that cause them to return identical or broken results.
</objective>

<context>
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stabilize KAP Scraper</name>
  <files>src/lib/kap/client.ts</files>
  <action>
    Review and update the `fetchDisclosures` function.
    Ensure `waitForSelector` has appropriate timeouts and error catching.
    Add extensive logging so that failures are captured in the pipeline logs.
  </action>
</task>

<task type="auto">
  <name>Task 2: Stabilize News Scrapers</name>
  <files>src/lib/news/bloomberg.ts, src/lib/news/ekonomim.ts</files>
  <action>
    Update DOM selectors to be more resilient (e.g., fallback selectors).
    Implement retry logic or graceful degradation if an article cannot be scraped.
  </action>
</task>

</tasks>

<success_criteria>
- The scrapers reliably fetch current data.
</success_criteria>
