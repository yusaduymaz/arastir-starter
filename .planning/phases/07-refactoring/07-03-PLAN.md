---
phase: "07"
plan: "03"
type: execute
wave: 3
depends_on: ["07-02"]
files_modified: ["src/components/dashboard/ActivePipeline.tsx", "src/app/dashboard/history/[id]/page.tsx"]
autonomous: true
requirements: ["UI-05"]

must_haves:
  truths:
    - "The dashboard accurately reflects pipeline status."
    - "The detailed report page displays the AI synthesis cleanly."
---

<objective>
Fix UI bugs on the dashboard and update the detail page to display the new AI-generated insights.
</objective>

<context>
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Active Pipeline UI</name>
  <files>src/components/dashboard/ActivePipeline.tsx</files>
  <action>
    Ensure the component polls the database correctly or handles the real `status` values without getting stuck.
  </action>
</task>

<task type="auto">
  <name>Task 2: Update Report Detail Page</name>
  <files>src/app/dashboard/history/[id]/page.tsx</files>
  <action>
    Update the "Sentez Özeti" (Synthesis Summary) section to display the AI's `executiveSummary`, `risks`, and `opportunities` instead of the old key-value list.
  </action>
</task>

</tasks>

<success_criteria>
- UI is stable and reflects the new AI data structure.
</success_criteria>
