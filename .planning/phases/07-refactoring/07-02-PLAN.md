---
phase: "07"
plan: "02"
type: execute
wave: 2
depends_on: ["07-01"]
files_modified: ["src/agents/analyst-agent.ts", "src/app/api/research/route.ts", "src/types/research.ts", "src/lib/pdf/generator.ts", "src/lib/pptx/generator.ts"]
autonomous: true
requirements: ["AI-01"]

must_haves:
  truths:
    - "An LLM is used to synthesize KAP and News data into a qualitative report."
    - "The hardcoded 'dandirik' table is replaced by dynamic AI insights."
---

<objective>
Implement a true AI Analyst Agent that uses an LLM to read the scraped raw data and generate a comprehensive, intelligent summary, removing the hardcoded fallback table.
</objective>

<context>
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Analyst Agent</name>
  <files>src/agents/analyst-agent.ts</files>
  <action>
    Create a new agent file `src/agents/analyst-agent.ts`.
    Implement `runAnalystAgent(ticker: string, kapData: any, newsData: any): Promise<any>`.
    Use the `@anthropic-ai/sdk` (or standard `fetch` to an LLM provider if preferred) to pass the stringified KAP and News data to the model.
    Instruct the model to act as a financial analyst and produce:
    1. A generated summary (Executive Summary).
    2. Key Risks.
    3. Opportunities.
    Return this structured insight.
  </action>
</task>

<task type="auto">
  <name>Task 2: Integrate Analyst into Pipeline</name>
  <files>src/app/api/research/route.ts</files>
  <action>
    In the `executeResearchPipeline` function, after fetching KAP and News data, call `runAnalystAgent`.
    Modify the `reportData` object structure to remove the hardcoded statistics array and instead include the AI-generated text fields (e.g., `executiveSummary`, `risks`, `opportunities`).
  </action>
</task>

<task type="auto">
  <name>Task 3: Update Generators</name>
  <files>src/lib/pdf/generator.ts, src/lib/pptx/generator.ts, src/types/research.ts</files>
  <action>
    Update `ReportData` interface in `src/types/research.ts` to include the new AI fields.
    Update the PDF HTML template and PPTX slide generator to render these text blocks instead of the old table.
  </action>
</task>

</tasks>

<success_criteria>
- The final reports contain actual AI-generated textual analysis.
</success_criteria>
