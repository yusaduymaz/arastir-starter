---
phase: "19"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - "src/components/landing/PricingSection.tsx"
  - "src/app/page.tsx"
autonomous: true
requirements: ["STRIPE-03"]

must_haves:
  truths:
    - "A `PricingSection` component renders 3 paid tiers + 1 Enterprise card in the Terminal aesthetic."
    - "The section is inserted into the landing page between OutputSection and CTASection."
    - "Fiyatlandırma nav link scrolls to the pricing section anchor."
    - "CTA buttons link to `/sign-up` (for unauthenticated) and `/dashboard/settings?tab=abonelik` (for authenticated users)."
---

<objective>
Build a high-converting, on-brand Pricing section for the landing page.
3 paid tiers (Starter, Pro, Agency) displayed as cards + 1 Enterprise contact row.
The existing CTASection (early access form) is kept but the Fiyatlandırma nav anchor is updated to point to the new section.
</objective>

<context>
## Current State
- Landing page renders: HeroSection → StatsBar → ComparisonSection → PipelineSection → OutputSection → CTASection
- The NAV_LINKS array in `src/app/page.tsx` has `{ href: '#cta', label: 'Fiyatlandırma' }` — this needs updating to `#pricing`
- Brand aesthetic: dark bg (#080808), green accent (#22c55e), yellow accent (#facc15), JetBrains Mono for labels, Montserrat for headings, Inter for body

## Pricing Tiers to Display
| Tier    | Tokens/ay | Fiyat     | Badge         | Highlight |
|---------|-----------|-----------|---------------|-----------|
| Starter | 30        | ₺299/ay   | -             | No        |
| Pro     | 100       | ₺799/ay   | EN POPÜLER    | Yes (yellow border) |
| Agency  | 500       | ₺2,499/ay | -             | No        |
| Enterprise | Özel  | Özel      | KURUMSAL      | No (different layout) |

## Feature List per Tier
All paid tiers include:
- KAP bildirimleri, haberler, makro veri
- PDF + PPTX rapor çıktısı
- Dashboard erişimi

Starter only:
- BIST hisse araştırması

Pro adds:
- Kripto & Altın & Döviz analizleri
- Öncelikli işleme kuyruğu

Agency adds:
- API erişimi (JSON output)
- Çoklu kullanıcı (5 koltuk)
- Öncelikli destek (< 4 saat)

Enterprise:
- Sınırsız araştırma
- SLA garantisi
- Özel entegrasyonlar
- Adanmış destek ekibi
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PricingSection Component</name>
  <files>src/components/landing/PricingSection.tsx</files>
  <action>
    Create `src/components/landing/PricingSection.tsx` as a client component with framer-motion animations.

    Structure:
    ```
    <section id="pricing">
      // Section header: "// Fiyatlandırma" label + "Araştırmanızı Güçlendirin" heading
      // 3-column card grid (Starter | Pro | Agency)
      // Enterprise full-width row below
    </section>
    ```

    **Card design for Starter, Pro, Agency:**
    - Dark card bg: `bg-[#080808]` with border `border-[#1a1a1b]`
    - Pro card (highlighted): border `border-[#facc15]/40` with subtle yellow glow shadow
    - Top of each card: Tier name + "EN POPÜLER" badge (only Pro)
    - Price: Large `font-['Montserrat'] font-black text-4xl text-white` with `/ay` suffix in smaller text
    - Token count line: `font-['JetBrains_Mono'] text-[#22c55e]` e.g. "30 araştırma / ay"
    - Feature list: Each item has a green checkmark icon (`check` material symbol) + Inter text
    - CTA Button:
      - Pro: `bg-[#facc15] text-[#0a0a0a]` (primary yellow)
      - Others: `border border-[#22c55e]/30 text-white hover:bg-[#22c55e]/10` (ghost green)
    - Button text: "Başla" for Starter/Agency, "Pro'ya Geç" for Pro

    **Enterprise row:**
    - Full width, different layout: horizontal flex, left side has text, right side has CTA
    - Text: "Enterprise · Kurumsal" heading + "Ekibiniz için özel fiyatlandırma ve SLA garanti" subtext
    - CTA: "Bizimle İletişime Geçin" button (outlined) linking to mailto or a contact anchor
    - Background: subtle gradient `from-[#0a0a0a] to-[#080808]` with green border

    **Animations:**
    - Section fades in with `whileInView` (same BEZIER as other sections)
    - Cards stagger: each card has `transition={{ delay: index * 0.1 }}`

    **Button behavior:**
    - All CTA buttons are `<Link>` components
    - If using `useUser()` from Clerk to detect auth state:
      - Signed in → link to `/dashboard/settings?tab=abonelik`
      - Signed out → link to `/sign-up`
    - Enterprise → `mailto:iletisim@arastir.com` or `#contact` anchor

    **Note on PLAN_CONFIG:** Import token counts and prices from `@/types/saas` PLAN_CONFIG constant to avoid hardcoding in the UI.

    Use this feature list structure:
    ```ts
    const PLANS = [
      {
        tier: 'starter' as Tier,
        features: [
          'BIST hisse araştırması',
          'KAP bildirimleri',
          'Haberler & sentiment',
          'PDF + PPTX rapor',
          'Dashboard erişimi',
        ],
      },
      {
        tier: 'pro' as Tier,
        badge: 'EN POPÜLER',
        features: [
          'Starter\'ın tüm özellikleri',
          'Kripto & Altın & Döviz',
          'Öncelikli işleme kuyruğu',
          'Makro ekonomi analizleri',
          'Gelişmiş AI sentezi',
        ],
      },
      {
        tier: 'agency' as Tier,
        features: [
          'Pro\'nun tüm özellikleri',
          'JSON API erişimi',
          '5 takım koltuğu',
          'Öncelikli destek (< 4s)',
          'Özel raporlama şablonları',
        ],
      },
    ]
    ```
  </action>
</task>

<task type="auto">
  <name>Task 2: Insert PricingSection into Landing Page</name>
  <files>src/app/page.tsx</files>
  <action>
    Modify `src/app/page.tsx`:

    1. Import the new component:
       ```ts
       import { PricingSection } from '@/components/landing/PricingSection';
       ```

    2. Insert `<PricingSection />` between `<OutputSection />` and the `<div id="cta">` block:
       ```tsx
       <OutputSection />
       <div id="pricing">
         <PricingSection />
       </div>
       <div id="cta">
         <CTASection />
       </div>
       ```

    3. Update the NAV_LINKS array — change the Fiyatlandırma href from `#cta` to `#pricing`:
       ```ts
       const NAV_LINKS = [
         { href: '#pipeline', label: 'Özellikler' },
         { href: '#pipeline', label: 'Nasıl Çalışır' },
         { href: '#pricing', label: 'Fiyatlandırma' },
       ]
       ```
  </action>
</task>

</tasks>

<success_criteria>
- PricingSection renders 3 tier cards + 1 Enterprise row.
- Pro card has yellow highlight border and "EN POPÜLER" badge.
- Token counts and prices are pulled from PLAN_CONFIG (no hardcoded numbers in the component).
- Fiyatlandırma nav link scrolls to `#pricing`.
- Component matches the dark terminal aesthetic of the rest of the landing page.
- No TypeScript errors.
</success_criteria>
