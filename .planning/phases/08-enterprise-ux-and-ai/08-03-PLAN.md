---
phase: "08"
plan: "03"
type: execute
wave: 3
depends_on: ["08-02"]
files_modified: ["src/agents/analyst-agent.ts", "src/lib/pdf/generator.ts", "package.json"]
autonomous: true
requirements: ["AI-02"]

must_haves:
  truths:
    - "Anthropic prompt utilizes advanced Chain of Thought structures."
    - "PDF Generator processes Markdown into beautiful HTML."
---

<objective>
Elevate the AI analysis from a basic summary to a deep, professional financial report using advanced prompting and Markdown rendering.
</objective>

<context>
@.planning/phases/08-enterprise-ux-and-ai/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Advanced Prompt Engineering</name>
  <files>src/agents/analyst-agent.ts</files>
  <action>
    Update `runAnalystAgent`.
    Instruct Claude to first think inside `<thought>` tags before generating the JSON.
    Ask for highly professional, markdown-formatted text for `executiveSummary`, `risks`, and `opportunities`.
  </action>
</task>

<task type="auto">
  <name>Task 2: Install Marked</name>
  <files>package.json</files>
  <action>
    Run `npm install marked` to convert the AI's markdown to HTML.
  </action>
</task>

<task type="auto">
  <name>Task 3: Polish PDF Design</name>
  <files>src/lib/pdf/generator.ts</files>
  <action>
    Import `marked`.
    Parse the AI insights via `marked.parse()`.
    Update the CSS in the PDF to look like a high-end McKinsey or Goldman Sachs report (elegant serif fonts, crisp margins, dark blue headers).
  </action>
</task>

</tasks>

<success_criteria>
- AI outputs are deep and insightful.
- PDF reports look like professional investment banking documents.
</success_criteria>
