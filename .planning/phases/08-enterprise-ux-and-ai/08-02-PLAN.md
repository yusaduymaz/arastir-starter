---
phase: "08"
plan: "02"
type: execute
wave: 2
depends_on: ["08-01"]
files_modified: ["src/app/api/research/route.ts"]
autonomous: true
requirements: ["SYS-01", "UX-01"]

must_haves:
  truths:
    - "The backend updates Supabase `progress` values during long-running tasks."
    - "The system checks for identical searches within 6 hours and serves from cache to save money."
---

<objective>
Refactor the main research API route to act like a true enterprise system: caching frequent queries and emitting granular progress updates.
</objective>

<context>
@.planning/phases/08-enterprise-ux-and-ai/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Caching Logic</name>
  <files>src/app/api/research/route.ts</files>
  <action>
    In the POST handler, before creating a new pending record, check if a completed record exists for the same `ticker` within the last 6 hours.
    If it exists, immediately create a cloned record for the current user with `status: 'completed'` and the same `result_path`, `kap_data`, etc.
    Return 202 with this cloned record ID, bypassing the pipeline entirely.
  </action>
</task>

<task type="auto">
  <name>Task 2: Emit Progress Updates</name>
  <files>src/app/api/research/route.ts</files>
  <action>
    In `executeResearchPipeline`, add intermediate `supabase.from().update()` calls.
    - Start: `progress: 10`, `current_step: 'Scraping Data'`
    - After KAP/News: `progress: 50`, `current_step: 'AI Analysis'`
    - After LLM: `progress: 80`, `current_step: 'Generating Reports'`
    - End: `progress: 100`, `status: 'completed'`
  </action>
</task>

</tasks>

<success_criteria>
- Cache hits instantly resolve.
- Long requests update the DB continuously.
</success_criteria>
