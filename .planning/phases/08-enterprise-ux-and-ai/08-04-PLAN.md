---
phase: "08"
plan: "04"
type: execute
wave: 4
depends_on: ["08-01", "08-02"]
files_modified: ["src/components/dashboard/ActivePipeline.tsx", "src/components/dashboard/HistoryTable.tsx"]
autonomous: true
requirements: ["UX-01", "UX-02"]

must_haves:
  truths:
    - "The UI updates progress bars in real-time."
    - "Users can delete rows via a UI button."
---

<objective>
Connect the frontend to the new backend capabilities to provide a seamless, live, and controllable experience.
</objective>

<context>
@.planning/phases/08-enterprise-ux-and-ai/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Supabase Realtime Pipeline</name>
  <files>src/components/dashboard/ActivePipeline.tsx</files>
  <action>
    Initialize a Supabase channel.
    Listen for updates to the `research_history` row.
    Animate the progress bar width based on the `progress` column.
    Update the label based on `current_step`.
    Trigger a full page reload or UI switch when `status === 'completed'`.
  </action>
</task>

<task type="auto">
  <name>Task 2: Delete Button in History</name>
  <files>src/components/dashboard/HistoryTable.tsx</files>
  <action>
    Add a Trash/Delete icon button to the "İşlemler" (Actions) column.
    Implement an onClick handler that calls the `DELETE /api/research/[id]` endpoint.
    Optimistically remove the row from the UI upon click.
  </action>
</task>

</tasks>

<success_criteria>
- Progress is no longer blind.
- History can be managed.
</success_criteria>
