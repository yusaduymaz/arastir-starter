---
phase: "08"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: ["supabase/migrations/20260513000004_progress_columns.sql", "src/app/api/research/[id]/route.ts"]
autonomous: true
requirements: ["UX-01", "UX-02"]

must_haves:
  truths:
    - "Database supports tracking exact progress (0-100) and step states."
    - "Users can delete reports via a new API endpoint."
---

<objective>
Setup database schema upgrades for real-time tracking and implement the deletion backend logic.
</objective>

<context>
@.planning/phases/08-enterprise-ux-and-ai/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Supabase Schema Updates</name>
  <files>supabase/migrations/20260513000004_progress_columns.sql</files>
  <action>
    Create a new migration file.
    Add columns to `research_history`:
    - `progress` (INTEGER DEFAULT 0)
    - `current_step` (TEXT DEFAULT 'initializing')
  </action>
</task>

<task type="auto">
  <name>Task 2: Delete API Endpoint</name>
  <files>src/app/api/research/[id]/route.ts</files>
  <action>
    Create a `DELETE` handler.
    Authenticate user via Clerk `auth()`.
    Query Supabase to ensure the record belongs to the user.
    If yes, `delete()` the row from `research_history`.
    Return 200 OK.
  </action>
</task>

</tasks>

<success_criteria>
- The database is ready for live progress inputs.
- A secure deletion mechanism exists.
</success_criteria>
