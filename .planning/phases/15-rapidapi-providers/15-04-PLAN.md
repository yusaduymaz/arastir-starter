---
phase: 15-rapidapi-providers
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified: [
  "src/lib/rapidapi/cryptonews/index.ts",
  "src/lib/rapidapi/cryptonews/types.ts",
  "src/lib/rapidapi/cryptonews/cryptonews.test.ts",
  "src/lib/rapidapi/yhfinance/index.ts",
  "src/lib/rapidapi/yhfinance/types.ts",
  "src/lib/rapidapi/yhfinance/yhfinance.test.ts"
]
autonomous: true
requirements: [RAPID-01, RAPID-04]
must_haves:
  truths:
    - Crypto News client fetches and normalizes data.
    - YH Finance client fetches and normalizes data.
    - All DTOs contain a provenance tag.
    - Mocked unit tests verify fetcher behavior.
  artifacts:
    - path: "src/lib/rapidapi/crypto_news/client.ts"
    - path: "src/lib/rapidapi/yh_finance/client.ts"
  key_links:
    - from: "src/lib/rapidapi/crypto_news/client.ts"
      to: "src/lib/rapidapi/_shared/fetcher.ts"
      via: "function call"
      pattern: "fetchRapidAPI"
---

<objective>
Implement RapidAPI clients for Crypto News and YH Finance.
Purpose: Expand the provider pool for crypto articles and Yahoo Finance data via Apidojo.
Output: Clients, types, endpoints, unit tests, and smoke tests.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/phases/15-rapidapi-providers/15-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crypto News Provider Client</name>
  <files>src/lib/rapidapi/crypto_news/client.ts, src/lib/rapidapi/crypto_news/types.ts, src/lib/rapidapi/crypto_news/endpoints.ts, src/lib/rapidapi/crypto_news/__tests__/smoke.test.ts, src/lib/rapidapi/crypto_news/__tests__/client.test.ts</files>
  <behavior>
    - Unit tests must mock the fetcher to verify param construction, cache hit/miss behavior, and error mapping.
    - The normalized response DTO must include `source: { provider: 'crypto_news', fetched_at: number, ttl_remaining: number }`.
    - Live smoke tests must be gated behind `process.env.RAPIDAPI_LIVE_TESTS === '1'`.
  </behavior>
  <action>
    Implement `types.ts` defining normalized DTOs. Ensure the `source` provenance tag is mandatory.
    Define endpoints in `endpoints.ts`.
    Implement `client.ts` using the shared `fetcher`.
    Write `client.test.ts` for unit tests with a mocked fetcher.
    Write `smoke.test.ts` to test against the real API.
  </action>
  <verify>
    <automated>npx vitest run src/lib/rapidapi/crypto_news/</automated>
  </verify>
  <done>DONE</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: YH Finance Provider Client</name>
  <files>src/lib/rapidapi/yh_finance/client.ts, src/lib/rapidapi/yh_finance/types.ts, src/lib/rapidapi/yh_finance/endpoints.ts, src/lib/rapidapi/yh_finance/__tests__/smoke.test.ts, src/lib/rapidapi/yh_finance/__tests__/client.test.ts</files>
  <behavior>
    - Unit tests mock the fetcher for validation.
    - Normalized responses contain `source: { provider: 'yh_finance', fetched_at: number, ttl_remaining: number }`.
    - Smoke tests gated by live flag.
  </behavior>
  <action>
    Implement `types.ts` with provenance tag, `endpoints.ts`, and `client.ts`.
    Write offline unit tests in `client.test.ts` and gated smoke tests in `smoke.test.ts`.
  </action>
  <verify>
    <automated>npx vitest run src/lib/rapidapi/yh_finance/</automated>
  </verify>
  <done>DONE</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| API -> Client | Untrusted JSON structures. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05 | Tampering | types.ts | mitigate | Robust mapping to local DTO shapes. |
</threat_model>
