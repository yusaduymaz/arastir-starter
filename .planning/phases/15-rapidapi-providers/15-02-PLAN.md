---
phase: 15-rapidapi-providers
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified: [
  "src/lib/rapidapi/cnbc/index.ts",
  "src/lib/rapidapi/cnbc/types.ts",
  "src/lib/rapidapi/cnbc/cnbc.test.ts",
  "src/lib/rapidapi/haremaltin/index.ts",
  "src/lib/rapidapi/haremaltin/types.ts",
  "src/lib/rapidapi/haremaltin/haremaltin.test.ts"
]
autonomous: true
requirements: [RAPID-01, RAPID-04]
must_haves:
  truths:
    - CNBC client fetches and normalizes news data.
    - Harem Altın client fetches and normalizes gold price data.
    - All DTOs contain a provenance tag.
    - Mocked unit tests verify fetcher param construction and error mapping.
  artifacts:
    - path: "src/lib/rapidapi/cnbc/client.ts"
    - path: "src/lib/rapidapi/harem_altin/client.ts"
  key_links:
    - from: "src/lib/rapidapi/cnbc/client.ts"
      to: "src/lib/rapidapi/_shared/fetcher.ts"
      via: "function call"
      pattern: "fetchRapidAPI"
---

<objective>
Implement RapidAPI clients for CNBC and Harem Altın.
Purpose: Provide typed and rate-limited clients for global news and Turkish gold prices.
Output: Clients, types, endpoints, unit tests, and smoke tests.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/phases/15-rapidapi-providers/15-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CNBC Provider Client</name>
  <files>src/lib/rapidapi/cnbc/client.ts, src/lib/rapidapi/cnbc/types.ts, src/lib/rapidapi/cnbc/endpoints.ts, src/lib/rapidapi/cnbc/__tests__/smoke.test.ts, src/lib/rapidapi/cnbc/__tests__/client.test.ts</files>
  <behavior>
    - Unit tests must mock the fetcher to verify param construction, cache hit/miss behavior, and error mapping.
    - The normalized response DTO must include `source: { provider: 'cnbc', fetched_at: number, ttl_remaining: number }`.
    - Live smoke tests must be gated behind `process.env.RAPIDAPI_LIVE_TESTS === '1'`.
  </behavior>
  <action>
    Implement `types.ts` defining normalized DTOs for CNBC news endpoints. Ensure the `source` provenance tag is mandatory in the type.
    Define endpoints in `endpoints.ts`.
    Implement `client.ts` using the shared `fetcher`.
    Write `client.test.ts` for unit tests with a mocked fetcher.
    Write `smoke.test.ts` to test against the real API when the live flag is set.
  </action>
  <verify>
    <automated>npx vitest run src/lib/rapidapi/cnbc/</automated>
  </verify>
  <done>DONE</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harem Altın Provider Client</name>
  <files>src/lib/rapidapi/harem_altin/client.ts, src/lib/rapidapi/harem_altin/types.ts, src/lib/rapidapi/harem_altin/endpoints.ts, src/lib/rapidapi/harem_altin/__tests__/smoke.test.ts, src/lib/rapidapi/harem_altin/__tests__/client.test.ts</files>
  <behavior>
    - Unit tests mock the fetcher for param, cache, and error validation.
    - Normalized responses contain `source: { provider: 'harem_altin', fetched_at: number, ttl_remaining: number }`.
    - Smoke tests gated by live flag.
  </behavior>
  <action>
    Implement `types.ts` with the provenance tag. Define endpoints.
    Implement `client.ts` using the shared fetcher to get gold prices (gram, çeyrek, etc).
    Write `client.test.ts` (unit) and `smoke.test.ts` (live).
  </action>
  <verify>
    <automated>npx vitest run src/lib/rapidapi/harem_altin/</automated>
  </verify>
  <done>DONE</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| API -> Client | Provider responses are untrusted. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03 | Tampering | types.ts | mitigate | Strong types and runtime checks for incoming API shape (via zod if needed, or defensive mapping). |
</threat_model>
