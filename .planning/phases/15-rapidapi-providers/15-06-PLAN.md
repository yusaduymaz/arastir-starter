---
phase: 15-rapidapi-providers
plan: 06
type: execute
wave: 2
depends_on: [01]
files_modified: [
  "src/lib/rapidapi/forexapi/index.ts",
  "src/lib/rapidapi/forexapi/types.ts",
  "src/lib/rapidapi/forexapi/forexapi.test.ts",
  "src/lib/rapidapi/turkeyfinancial/index.ts",
  "src/lib/rapidapi/turkeyfinancial/types.ts",
  "src/lib/rapidapi/turkeyfinancial/turkeyfinancial.test.ts"
]
autonomous: true
requirements: [RAPID-01, RAPID-04]
must_haves:
  truths:
    - Forex API client fetches and normalizes data.
    - Turkey Financial Data API client fetches and normalizes data.
    - All DTOs contain a provenance tag.
    - Mocked unit tests verify fetcher behavior.
  artifacts:
    - path: "src/lib/rapidapi/forex/client.ts"
    - path: "src/lib/rapidapi/turkey_financial/client.ts"
  key_links:
    - from: "src/lib/rapidapi/forex/client.ts"
      to: "src/lib/rapidapi/_shared/fetcher.ts"
      via: "function call"
      pattern: "fetchRapidAPI"
---

<objective>
Implement RapidAPI clients for Forex API and Turkey Financial Data API.
Purpose: Expand the provider pool for forex pairs and BIST equities data.
Output: Clients, types, endpoints, unit tests, and smoke tests.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/phases/15-rapidapi-providers/15-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Forex API Provider Client</name>
  <files>src/lib/rapidapi/forex/client.ts, src/lib/rapidapi/forex/types.ts, src/lib/rapidapi/forex/endpoints.ts, src/lib/rapidapi/forex/__tests__/smoke.test.ts, src/lib/rapidapi/forex/__tests__/client.test.ts</files>
  <behavior>
    - Unit tests must mock the fetcher to verify param construction, cache hit/miss behavior, and error mapping.
    - The normalized response DTO must include `source: { provider: 'forex', fetched_at: number, ttl_remaining: number }`.
    - Live smoke tests must be gated behind `process.env.RAPIDAPI_LIVE_TESTS === '1'`.
  </behavior>
  <action>
    Implement `types.ts` defining normalized DTOs. Ensure the `source` provenance tag is mandatory.
    Define endpoints in `endpoints.ts`.
    Implement `client.ts` using the shared `fetcher`.
    Write `client.test.ts` for unit tests with a mocked fetcher.
    Write `smoke.test.ts` to test against the real API.
  </action>
  <verify>
    <automated>npx vitest run src/lib/rapidapi/forex/</automated>
  </verify>
  <done>DONE</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Turkey Financial Data API Provider Client</name>
  <files>src/lib/rapidapi/turkey_financial/client.ts, src/lib/rapidapi/turkey_financial/types.ts, src/lib/rapidapi/turkey_financial/endpoints.ts, src/lib/rapidapi/turkey_financial/__tests__/smoke.test.ts, src/lib/rapidapi/turkey_financial/__tests__/client.test.ts</files>
  <behavior>
    - Unit tests mock the fetcher for validation.
    - Normalized responses contain `source: { provider: 'turkey_financial', fetched_at: number, ttl_remaining: number }`.
    - Smoke tests gated by live flag.
  </behavior>
  <action>
    Implement `types.ts` with provenance tag, `endpoints.ts`, and `client.ts`.
    Write offline unit tests in `client.test.ts` and gated smoke tests in `smoke.test.ts`.
  </action>
  <verify>
    <automated>npx vitest run src/lib/rapidapi/turkey_financial/</automated>
  </verify>
  <done>DONE</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| API -> Client | Untrusted JSON structures. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-07 | Tampering | types.ts | mitigate | Robust mapping to local DTO shapes. |
</threat_model>
