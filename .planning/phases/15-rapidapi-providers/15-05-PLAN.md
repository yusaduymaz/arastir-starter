---
phase: 15-rapidapi-providers
plan: 05
type: execute
wave: 2
depends_on: [01]
files_modified: [
  "src/lib/rapidapi/yahoorealtime/index.ts",
  "src/lib/rapidapi/yahoorealtime/types.ts",
  "src/lib/rapidapi/yahoorealtime/yahoorealtime.test.ts",
  "src/lib/rapidapi/cryptorsi/index.ts",
  "src/lib/rapidapi/cryptorsi/types.ts",
  "src/lib/rapidapi/cryptorsi/cryptorsi.test.ts"
]
autonomous: true
requirements: [RAPID-01, RAPID-04]
must_haves:
  truths:
    - Yahoo Real Time client fetches and normalizes data.
    - Crypto RSI client fetches and normalizes data.
    - All DTOs contain a provenance tag.
    - Mocked unit tests verify fetcher behavior.
  artifacts:
    - path: "src/lib/rapidapi/yahoo_realtime/client.ts"
    - path: "src/lib/rapidapi/crypto_rsi/client.ts"
  key_links:
    - from: "src/lib/rapidapi/yahoo_realtime/client.ts"
      to: "src/lib/rapidapi/_shared/fetcher.ts"
      via: "function call"
      pattern: "fetchRapidAPI"
---

<objective>
Implement RapidAPI clients for Yahoo Finance Real Time and Crypto Trading Indicators API-RSI.
Purpose: Expand the provider pool for real-time quotes and crypto technical analysis.
Output: Clients, types, endpoints, unit tests, and smoke tests.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/phases/15-rapidapi-providers/15-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Yahoo Finance Real Time Provider Client</name>
  <files>src/lib/rapidapi/yahoo_realtime/client.ts, src/lib/rapidapi/yahoo_realtime/types.ts, src/lib/rapidapi/yahoo_realtime/endpoints.ts, src/lib/rapidapi/yahoo_realtime/__tests__/smoke.test.ts, src/lib/rapidapi/yahoo_realtime/__tests__/client.test.ts</files>
  <behavior>
    - Unit tests must mock the fetcher to verify param construction, cache hit/miss behavior, and error mapping.
    - The normalized response DTO must include `source: { provider: 'yahoo_realtime', fetched_at: number, ttl_remaining: number }`.
    - Live smoke tests must be gated behind `process.env.RAPIDAPI_LIVE_TESTS === '1'`.
  </behavior>
  <action>
    Implement `types.ts` defining normalized DTOs. Ensure the `source` provenance tag is mandatory.
    Define endpoints in `endpoints.ts`.
    Implement `client.ts` using the shared `fetcher`.
    Write `client.test.ts` for unit tests with a mocked fetcher.
    Write `smoke.test.ts` to test against the real API.
  </action>
  <verify>
    <automated>npx vitest run src/lib/rapidapi/yahoo_realtime/</automated>
  </verify>
  <done>DONE</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Crypto Trading Indicators API-RSI Provider Client</name>
  <files>src/lib/rapidapi/crypto_rsi/client.ts, src/lib/rapidapi/crypto_rsi/types.ts, src/lib/rapidapi/crypto_rsi/endpoints.ts, src/lib/rapidapi/crypto_rsi/__tests__/smoke.test.ts, src/lib/rapidapi/crypto_rsi/__tests__/client.test.ts</files>
  <behavior>
    - Unit tests mock the fetcher for validation.
    - Normalized responses contain `source: { provider: 'crypto_rsi', fetched_at: number, ttl_remaining: number }`.
    - Smoke tests gated by live flag.
  </behavior>
  <action>
    Implement `types.ts` with provenance tag, `endpoints.ts`, and `client.ts`.
    Write offline unit tests in `client.test.ts` and gated smoke tests in `smoke.test.ts`.
  </action>
  <verify>
    <automated>npx vitest run src/lib/rapidapi/crypto_rsi/</automated>
  </verify>
  <done>DONE</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| API -> Client | Untrusted JSON structures. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-06 | Tampering | types.ts | mitigate | Robust mapping to local DTO shapes. |
</threat_model>
