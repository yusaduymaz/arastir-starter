---
phase: 22-rapor-kalitesi-veri-zenginlestirme
plan: 02
type: execute
wave: 2
depends_on:
  - 22-00
  - 22-01
  - 22-03
files_modified:
  - src/app/dashboard/history/[id]/page.tsx
autonomous: true
requirements:
  - RPT-03
  - RPT-04

must_haves:
  truths:
    - "Piyasa verisi kartı başlığı yanında 'X dakika önce güncellendi' veya 'Az önce güncellendi' zaman damgası gösterir"
    - "Makro göstergeler bölümü başlığında TCMB agent'ının tamamlanma zamanını gösteren zaman damgası var"
    - "Haber duygu analizi bölümü başlığında haber agent tamamlanma zamanını gösteren zaman damgası var"
    - "Temel Göstergeler bölümünde P/B, ROE, ROA, Beta, Net Marj, Halka Açık Hisse Adedi satırları listeleniyor"
    - "Eksik rasyo alanları '-' ile gösteriliyor, boş string veya undefined gösterilmiyor"
  artifacts:
    - path: "src/app/dashboard/history/[id]/page.tsx"
      provides: "timeAgo helper, agent_runs timestamp sorgusu, zaman damgası UI, yeni rasyo kartı"
      contains: "timeAgo"
  key_links:
    - from: "src/app/dashboard/history/[id]/page.tsx"
      to: "market_data.source.fetched_at"
      via: "timeAgo(market_data.source.fetched_at)"
      pattern: "timeAgo"
    - from: "src/app/dashboard/history/[id]/page.tsx"
      to: "agent_runs tablosu"
      via: "supabase query agent_name='macro'"
      pattern: "agent_runs"
---

<objective>
Bu plan iki görevi kapsar: (1) piyasa, makro ve haber veri bloklarına "X dakika önce güncellendi" zaman damgası eklemek; (2) Temel Göstergeler bölümüne Plan 22-03'ün getirdiği P/B, ROE, ROA, Beta, Net Marj, Halka Açık Hisse Adedi rasyolarını görüntülemek.

Purpose: Kullanıcı rapor sayfasında verinin tazeliğini tek bakışta anlayabilmeli; finansal rasyoları Temel Göstergeler kartında görmelidir.

Output:
- `history/[id]/page.tsx` — timeAgo() helper, agent_runs timestamp sorgusu (macro + news), zaman damgası UI, yeni rasyo kartı
</objective>

<execution_context>
@C:/Users/duyma/Desktop/arastir-starter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/duyma/Desktop/arastir-starter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/duyma/Desktop/arastir-starter/.planning/ROADMAP.md
@C:/Users/duyma/Desktop/arastir-starter/.planning/STATE.md
@C:/Users/duyma/Desktop/arastir-starter/.planning/phases/22-rapor-kalitesi-veri-zenginlestirme/22-03-SUMMARY.md

<interfaces>
<!-- market_data.source.fetched_at mevcut tip (src/types/market.ts satır 46-54) -->
```typescript
export interface MarketData {
  quote: StockQuote;
  overview: CompanyOverview | null;
  monthlySeries: Record<string, MonthlySeriesEntry>;
  source: {
    provider: string;
    fetched_at: number;  // Unix ms — Date.now() döndürülüyor
    ttl_remaining: number;
  };
}
```

<!-- 22-03-PLAN.md sonrası CompanyOverview yeni alanları -->
```typescript
// src/types/market.ts (22-03 planı sonrası)
export const CompanyOverviewSchema = z.object({
  // ... mevcut alanlar ...
  PBRatio: z.string().default(''),       // F/DD
  Beta: z.string().default(''),
  FloatShares: z.string().default(''),   // Halka Açık Hisse Adedi
  ROE: z.string().default(''),           // % olarak (örn: '23.45')
  ROA: z.string().default(''),           // % olarak
  NetMargin: z.string().default(''),     // % olarak
}).passthrough();
```

<!-- agent_runs tablosu şeması — completed_at sütunu mevcut -->
```typescript
// supabase query örneği:
const { data: agentRuns } = await supabase
  .from('agent_runs')
  .select('agent_name, completed_at')
  .eq('session_id', params.id)
  .in('agent_name', ['macro', 'news'])
  .not('completed_at', 'is', null);
// agentRuns[i].completed_at: string (ISO8601)
```

<!-- history/[id]/page.tsx mevcut import ve helper satırları (satır 1-22) -->
```typescript
import React from 'react';
import { auth } from '@clerk/nextjs/server';
import { createClient } from '@supabase/supabase-js';
// ... diğer importlar ...

function formatMarketCap(val: string | undefined): string { ... }
function getLatestNonNull(data: any[], field: string): string | null { ... }
// timeAgo() henüz YOK — bu plan ekleyecek
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Görev 1: timeAgo helper + agent_runs timestamp sorgusu + zaman damgası UI</name>
  <files>src/app/dashboard/history/[id]/page.tsx</files>
  <read_first>
    - src/app/dashboard/history/[id]/page.tsx (tam dosya — Görev 22-01 sonrası güncellenmiş hali)
  </read_first>
  <behavior>
    - Test 1: timeAgo(Date.now() - 30000) → 'Az önce güncellendi' (30 saniye önce, 1 dakikadan az)
    - Test 2: timeAgo(Date.now() - 5 * 60 * 1000) → '5 dakika önce güncellendi'
    - Test 3: timeAgo(Date.now() - 90 * 60 * 1000) → '2 saat önce güncellendi'
  </behavior>
  <action>
    Dosyaya `formatMarketCap` ve `getLatestNonNull` fonksiyonlarının altına şu helper'ı ekle:

    ```typescript
    function timeAgo(fetchedAt: number | string): string {
      const ts = typeof fetchedAt === 'string' ? new Date(fetchedAt).getTime() : fetchedAt;
      const diffMs = Date.now() - ts;
      const diffMin = Math.round(diffMs / 60000);
      if (diffMin < 1) return 'Az önce güncellendi';
      if (diffMin < 60) return `${diffMin} dakika önce güncellendi`;
      const diffHr = Math.round(diffMin / 60);
      return `${diffHr} saat önce güncellendi`;
    }
    ```

    Supabase sorgusu bölümüne (report sorgusu yapılan try bloğunun hemen ardına, return ifadesinden önce) şu ek sorguyu ekle:

    ```typescript
    const { data: agentTimestamps } = await supabase
      .from('agent_runs')
      .select('agent_name, completed_at')
      .eq('session_id', params.id)
      .in('agent_name', ['macro', 'news'])
      .not('completed_at', 'is', null);

    const macroCompletedAt = agentTimestamps?.find(r => r.agent_name === 'macro')?.completed_at ?? null;
    const newsCompletedAt = agentTimestamps?.find(r => r.agent_name === 'news')?.completed_at ?? null;
    ```

    Piyasa verisi (quote) bölümünün header kısmına (Row 1 grid div'i, `// Guncel Fiyat` span'ının bulunduğu satıra değil; Row 1 grid'in üstüne veya Rapor İstatistikleri kartındaki "Veri Kaynağı" satırı altına) zaman damgası ekle:

    Rapor İstatistikleri kartındaki `market_data?.source?.provider` bloğunun hemen altına şu satırı ekle:
    ```tsx
    {market_data?.source?.fetched_at && (
      <div className="flex justify-between items-center">
        <span className="font-['JetBrains_Mono'] text-[11px] text-[#64748b]">Piyasa Verisi</span>
        <span className="font-['JetBrains_Mono'] text-[10px] text-[#45474c]">
          {timeAgo(market_data.source.fetched_at)}
        </span>
      </div>
    )}
    ```

    Makro göstergeler bölümünün header kısmında (`// Makro Gostergeler (TCMB EVDS)` span'ının hemen altına) şu satırı ekle:
    ```tsx
    {macroCompletedAt && (
      <span className="font-['JetBrains_Mono'] text-[9px] text-[#45474c]">
        {timeAgo(macroCompletedAt)}
      </span>
    )}
    ```

    Haber duygu analizi bölümünün header kısmında (`// Haber Duygu Analizi` span'ının hemen altına) şu satırı ekle:
    ```tsx
    {newsCompletedAt && (
      <span className="font-['JetBrains_Mono'] text-[9px] text-[#45474c]">
        {timeAgo(newsCompletedAt)}
      </span>
    )}
    ```
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "history"</automated>
    <automated>npx vitest run tests/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "timeAgo" src/app/dashboard/history/\[id\]/page.tsx` — en az 4 eşleşme (tanım + 3 kullanım)
    - `grep -n "agent_runs" src/app/dashboard/history/\[id\]/page.tsx` — en az 1 eşleşme
    - `grep -n "macroCompletedAt\|newsCompletedAt" src/app/dashboard/history/\[id\]/page.tsx` — en az 4 eşleşme
    - TypeScript derleme hatası yoktur
    - npx vitest run tests/ geçiyor (tests/lib/time-ago.test.ts testleri yeşil)
  </acceptance_criteria>
  <done>Piyasa, makro ve haber veri bloklarının her birinde timeAgo ile hesaplanan zaman damgası gösteriliyor; vitest geçiyor.</done>
</task>

<task type="auto">
  <name>Görev 2: Temel Finansal Rasyolar kartı</name>
  <files>src/app/dashboard/history/[id]/page.tsx</files>
  <read_first>
    - src/app/dashboard/history/[id]/page.tsx (Görev 1 sonrası güncellenmiş hali — özellikle Row 1 ve Row 4 bölümleri)
  </read_first>
  <action>
    Row 4 bölümünde `// Rapor Istatistikleri` kartının hemen üstüne (veya aynı `.flex.flex-col.gap-4` div'in içine, Rapor İstatistikleri kartından önce) yeni Temel Göstergeler kartını ekle:

    ```tsx
    {/* Temel Finansal Göstergeler */}
    {overview && (overview.PBRatio || overview.ROE || overview.ROA || overview.Beta || overview.NetMargin || overview.FloatShares) && (
      <div className="bg-[#080808] border border-[#c084fc]/12 rounded-xl p-5">
        <span className="font-['JetBrains_Mono'] text-[9px] text-[#c084fc]/50 tracking-widest uppercase mb-3 block">
          // Temel Gostergeler
        </span>
        <div className="space-y-2.5">
          {[
            { label: 'F/DD (P/B)', value: overview.PBRatio },
            { label: 'Beta', value: overview.Beta },
            { label: 'ROE (Ozk. Karlılığı)', value: overview.ROE ? `%${overview.ROE}` : '' },
            { label: 'ROA (Varlık Karlılığı)', value: overview.ROA ? `%${overview.ROA}` : '' },
            { label: 'Net Kar Marjı', value: overview.NetMargin ? `%${overview.NetMargin}` : '' },
            { label: 'Halka Açık Hisse Adedi', value: overview.FloatShares ? formatMarketCap(overview.FloatShares).replace(' TL', ' hisse') : '' },
          ].map(({ label, value }) => (
            <div key={label} className="flex justify-between items-center">
              <span className="font-['JetBrains_Mono'] text-[11px] text-[#64748b]">{label}</span>
              <span className="font-['Montserrat'] text-sm font-bold text-[#c084fc]">
                {value && value !== '' ? value : '-'}
              </span>
            </div>
          ))}
        </div>
      </div>
    )}
    ```

    DİKKAT:
    - `FloatShares` için `formatMarketCap(overview.FloatShares)` helper'ını yeniden kullan ama TL etiketini "hisse" ile değiştir — bu sayede büyük sayılar "500M hisse" formatında gösterilir
    - `formatMarketCap` zaten dosyada tanımlı, yeni import gerekmez
    - Kart yalnızca altı değerden en az biri dolu olduğunda görünür (tamamen boş kart gösterilmez)
    - Her değer boşsa veya undefined ise '-' gösterilir (hiç patlama olmaz)
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "history"</automated>
    <automated>npx vitest run tests/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Temel Gostergeler\|PBRatio\|ROE\|ROA\|NetMargin\|FloatShares\|Halka Açık" src/app/dashboard/history/\[id\]/page.tsx` — en az 6 eşleşme
    - Her rasyo satırında `value && value !== '' ? value : '-'` veya eşdeğer null-safe pattern mevcut
    - `formatMarketCap` hisse adedi gösterimi için yeniden kullanılıyor
    - TypeScript derleme hatası yoktur
    - npx vitest run tests/ geçiyor
  </acceptance_criteria>
  <done>Temel Göstergeler kartı P/B, Beta, ROE, ROA, Net Marj ve Halka Açık Hisse Adedi satırlarını gösteriyor; eksik değerler '-' olarak görünüyor; vitest geçiyor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent_runs.completed_at → timeAgo() | DB'den gelen ISO8601 string; geçersiz tarih olabilir |
| overview.FloatShares → formatMarketCap | Yahoo'dan gelen sayısal string; geçersiz olabilir |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-02-01 | Tampering | timeAgo(completed_at) — geçersiz tarih string | mitigate | timeAgo() içinde new Date(fetchedAt).getTime() NaN döndürürse diffMin Infinity olur → 'saat önce' göstenir; kullanıcı zararı minimal; statik veri gösterimi |
| T-22-02-02 | Denial of Service | ek agent_runs sorgusu | accept | SELECT sorgusu, 2 satır max; session_id + agent_name index'li; önemsiz yük |
| T-22-02-03 | Information Disclosure | overview rasyoları null karşılaştırması | accept | Tüm değerler fmt() ile string'e dönüştürülmüş; XSS riski yok (React DOM escaping) |
</threat_model>

<verification>
1. `npx tsc --noEmit` — TypeScript hata yoktur
2. `npx vitest run tests/` — tüm test dosyaları geçiyor
3. `grep -n "timeAgo" src/app/dashboard/history/\[id\]/page.tsx` — en az 4 eşleşme
4. `grep -n "agent_runs" src/app/dashboard/history/\[id\]/page.tsx` — en az 1 eşleşme
5. `grep -n "Temel Gostergeler" src/app/dashboard/history/\[id\]/page.tsx` — 1 eşleşme
6. `grep -n "Halka Açık" src/app/dashboard/history/\[id\]/page.tsx` — 1 eşleşme
7. `grep -n "PBRatio\|ROE\|ROA\|Halka" src/app/dashboard/history/\[id\]/page.tsx` — en az 4 eşleşme
</verification>

<success_criteria>
1. Piyasa verisi, makro göstergeler ve haber analizi bloklarının her birinde "X dakika önce güncellendi" veya "Az önce güncellendi" zaman damgası görünür.
2. Temel Göstergeler kartı (P/B, Beta, ROE, ROA, Net Marj, Halka Açık Hisse Adedi) rapor sayfasında listelenir.
3. Rasyo değeri yoksa satır '-' gösterir; bileşen hiçbir durumda patlamaz.
4. TypeScript derleme hatası yoktur.
5. `FloatShares` yüzde değil hisse adedi olarak etiketlenmiştir ("Halka Açık Hisse Adedi").
6. npx vitest run tests/ geçiyor.
</success_criteria>

<output>
Tamamlandıktan sonra `.planning/phases/22-rapor-kalitesi-veri-zenginlestirme/22-02-SUMMARY.md` oluştur.
</output>
