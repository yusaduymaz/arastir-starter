---
phase: 22-rapor-kalitesi-veri-zenginlestirme
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/market.ts
  - src/lib/market/yahoo-client.ts
autonomous: true
requirements:
  - RPT-04

must_haves:
  truths:
    - "yahoo-client.ts quoteSummary çağrısı 'financialData' modülünü içerir"
    - "CompanyOverviewSchema PBRatio, Beta, FloatShares, ROE, ROA, NetMargin alanlarını tanımlar"
    - "ROE, ROA, NetMargin Yahoo'dan gelen 0-1 oran değeri 100 ile çarpılarak yüzde olarak saklanır"
    - "floatShares null veya undefined geldiğinde boş string ('') döner, patlama olmaz"
  artifacts:
    - path: "src/types/market.ts"
      provides: "CompanyOverviewSchema'ya PBRatio, Beta, FloatShares, ROE, ROA, NetMargin eklenmiş"
      contains: "PBRatio"
    - path: "src/lib/market/yahoo-client.ts"
      provides: "financialData modülü eklenmiş, yeni alanlar mapping'e dahil"
      contains: "financialData"
  key_links:
    - from: "src/lib/market/yahoo-client.ts"
      to: "src/types/market.ts"
      via: "CompanyOverview type cast"
      pattern: "as CompanyOverview"
---

<objective>
Yahoo Finance `quoteSummary` çağrısına `financialData` modülünü ekle; `defaultKeyStatistics`'ten P/B, Beta, FloatShares alanlarını çek; `financialData`'dan ROE, ROA, NetMargin alanlarını çekip 0-1 → yüzde dönüşümü uygula. Tip tanımlarını güncelle.

Purpose: Plan 22-02'nin frontend'de bu alanları gösterebilmesi için veri altyapısını hazırla. Yeni araştırma oturumlarından itibaren bu alanlar `market_data.overview` JSON'una dahil edilecek.

Output:
- `src/types/market.ts` — CompanyOverviewSchema 6 yeni alan
- `src/lib/market/yahoo-client.ts` — financialData modülü + mapping
</objective>

<execution_context>
@C:/Users/duyma/Desktop/arastir-starter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/duyma/Desktop/arastir-starter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/duyma/Desktop/arastir-starter/.planning/ROADMAP.md
@C:/Users/duyma/Desktop/arastir-starter/.planning/STATE.md

<interfaces>
<!-- src/types/market.ts mevcut CompanyOverviewSchema (satır 16-31) -->
```typescript
export const CompanyOverviewSchema = z.object({
  Symbol: z.string(),
  Name: z.string().default(''),
  Description: z.string().default(''),
  Exchange: z.string().default(''),
  Currency: z.string().default(''),
  Country: z.string().default(''),
  Sector: z.string().default(''),
  Industry: z.string().default(''),
  MarketCapitalization: z.string().default(''),
  PERatio: z.string().default(''),
  EPS: z.string().default(''),
  DividendYield: z.string().default(''),
  '52WeekHigh': z.string().default(''),
  '52WeekLow': z.string().default(''),
}).passthrough();  // <-- passthrough mevcut, yeni alanlar eklenebilir
```

<!-- src/lib/market/yahoo-client.ts mevcut quoteSummary çağrısı (satır 55-81) -->
```typescript
const summary: any = await yf.quoteSummary(
  symbol,
  { modules: ['summaryDetail', 'price', 'assetProfile', 'defaultKeyStatistics'] },
  // EKSIK: 'financialData' modülü yok
  { validateResult: false }
);

const profile = summary?.assetProfile || {};
const price = summary?.price || {};
const detail = summary?.summaryDetail || {};
const stats = summary?.defaultKeyStatistics || {};
// EKSIK: const fd = summary?.financialData || {};

overview = {
  // ... mevcut alanlar ...
  EPS: fmt(stats.trailingEps?.raw ?? stats.trailingEps),
  // EKSIK: PBRatio, Beta, FloatShares, ROE, ROA, NetMargin
} as CompanyOverview;
```

<!-- fmt() helper (satır 14-18) -->
```typescript
const fmt = (n: unknown): string => {
  if (n === null || n === undefined) return '';
  if (typeof n === 'number' && Number.isFinite(n)) return String(n);
  return String(n);
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Görev 1: CompanyOverviewSchema'ya yeni rasyo alanları ekle</name>
  <files>src/types/market.ts</files>
  <read_first>
    - src/types/market.ts (tam dosya — mevcut schema alanları)
  </read_first>
  <behavior>
    - Test 1: CompanyOverviewSchema.parse({Symbol: 'TEST'}) başarıyla döner ve PBRatio, Beta, FloatShares, ROE, ROA, NetMargin alanları boş string default değeriyle mevcut
    - Test 2: CompanyOverviewSchema.parse({Symbol: 'TEST', PBRatio: '2.5', ROE: '23.45'}) — PBRatio ve ROE değerleri korunur
    - Test 3: CompanyOverview tipi `PBRatio` özelliğine erişim TypeScript'te hata vermez
  </behavior>
  <action>
    `CompanyOverviewSchema` z.object() tanımına `'52WeekLow': z.string().default('')` satırından sonra şu 6 alanı ekle:

    ```
    PBRatio: z.string().default(''),       // F/DD (Fiyat/Defter Değeri)
    Beta: z.string().default(''),          // Beta katsayısı
    FloatShares: z.string().default(''),   // Halka Açık Hisse Adedi
    ROE: z.string().default(''),           // Özkaynak Karlılığı (%)
    ROA: z.string().default(''),           // Varlık Karlılığı (%)
    NetMargin: z.string().default(''),     // Net Kar Marjı (%)
    ```

    `.passthrough()` satırını koru — silinmemeli.

    `CompanyOverview` tipi z.infer ile türetildiği için otomatik güncellenir, ayrıca type tanımına dokunma.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "market.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "PBRatio" src/types/market.ts` — 1 eşleşme döner
    - `grep -n "FloatShares" src/types/market.ts` — 1 eşleşme döner
    - `grep -n "NetMargin" src/types/market.ts` — 1 eşleşme döner
    - `.passthrough()` hâlâ schema sonunda mevcut
    - TypeScript derleme hatası yoktur
  </acceptance_criteria>
  <done>CompanyOverviewSchema 6 yeni alan içeriyor; CompanyOverview tipi otomatik genişledi.</done>
</task>

<task type="auto" tdd="true">
  <name>Görev 2: yahoo-client.ts financialData modülü ve mapping</name>
  <files>src/lib/market/yahoo-client.ts</files>
  <read_first>
    - src/lib/market/yahoo-client.ts (tam dosya — quoteSummary çağrısı ve overview mapping satırları)
    - src/types/market.ts (Görev 1 sonrası güncellenmiş — CompanyOverview tipi)
  </read_first>
  <behavior>
    - Test 1: modules dizisi 'financialData' içerir
    - Test 2: fd.returnOnEquity = 0.23 → overview.ROE = '23' (fmt(0.23 * 100) = '23')
    - Test 3: fd.returnOnEquity = null → overview.ROE = '' (boş string, patlama yok)
    - Test 4: stats.floatShares = {raw: 500000000} → overview.FloatShares = '500000000'
    - Test 5: stats.priceToBook = null → overview.PBRatio = ''
  </behavior>
  <action>
    quoteSummary çağrısındaki modules dizisini güncelle:
    - `['summaryDetail', 'price', 'assetProfile', 'defaultKeyStatistics']`
    - → `['summaryDetail', 'price', 'assetProfile', 'defaultKeyStatistics', 'financialData']`

    stats değişkeninin hemen altına şu satırı ekle:
    ```
    const fd = summary?.financialData || {};
    ```

    overview objesinin mapping kısmına (EPS satırından sonra) şu alanları ekle:
    ```
    PBRatio: fmt(stats.priceToBook?.raw ?? stats.priceToBook),
    Beta: fmt(stats.beta?.raw ?? stats.beta),
    FloatShares: fmt(stats.floatShares?.raw ?? stats.floatShares),
    ROE: fd.returnOnEquity != null
      ? fmt(((fd.returnOnEquity?.raw ?? fd.returnOnEquity) * 100).toFixed(2))
      : '',
    ROA: fd.returnOnAssets != null
      ? fmt(((fd.returnOnAssets?.raw ?? fd.returnOnAssets) * 100).toFixed(2))
      : '',
    NetMargin: fd.profitMargins != null
      ? fmt(((fd.profitMargins?.raw ?? fd.profitMargins) * 100).toFixed(2))
      : '',
    ```

    DİKKAT: `as CompanyOverview` cast'i satırı aynı kalır. Başka hiçbir satıra dokunma.
    DİKKAT: ROE/ROA/NetMargin null kontrolü `!= null` kullan (0 değerini falsy saymayacak şekilde).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "yahoo-client"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "financialData" src/lib/market/yahoo-client.ts` — en az 2 eşleşme (modules dizisinde ve `const fd =` satırında)
    - `grep -n "returnOnEquity" src/lib/market/yahoo-client.ts` — 1 eşleşme
    - `grep -n "profitMargins" src/lib/market/yahoo-client.ts` — 1 eşleşme
    - `grep -n "priceToBook" src/lib/market/yahoo-client.ts` — 1 eşleşme
    - TypeScript derleme hatası yoktur
  </acceptance_criteria>
  <done>getYahooMarketData çıktısı artık PBRatio, Beta, FloatShares, ROE, ROA, NetMargin alanlarını içeriyor; eksik değerler boş string döner.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Yahoo Finance API → yahoo-client.ts | Dış kaynak veri; null/undefined/unexpected shape olabilir |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03-01 | Tampering | fd.returnOnEquity * 100 çarpımı | mitigate | `!= null` kontrolü ile null/undefined guard; `.raw ?? value` ile ham değer alımı; fmt() son katman olarak string'e çevirir |
| T-22-03-02 | Denial of Service | financialData modülü ek API yükü | accept | quoteSummary tek çağrıda tüm modülleri alıyor; ek network çağrısı yok; Yahoo rate limit yoksa sorun yok |
</threat_model>

<verification>
1. `npx tsc --noEmit` — TypeScript hata yoktur
2. `grep -n "financialData" src/lib/market/yahoo-client.ts` — en az 2 eşleşme
3. `grep -n "PBRatio\|Beta\|FloatShares\|ROE\|ROA\|NetMargin" src/types/market.ts` — 6 eşleşme
4. `grep -n "returnOnEquity\|returnOnAssets\|profitMargins" src/lib/market/yahoo-client.ts` — 3 eşleşme
</verification>

<success_criteria>
1. `CompanyOverviewSchema` ve `CompanyOverview` tipi PBRatio, Beta, FloatShares, ROE, ROA, NetMargin alanlarını içerir.
2. `getYahooMarketData` çağrısı artık `financialData` modülünü de çeker.
3. ROE/ROA/NetMargin değerleri 0-1 oran olarak değil, 100 ile çarpılmış yüzde olarak saklanır.
4. Tüm yeni alanlar için Yahoo Finance null/undefined döndürdüğünde boş string ('') saklanır; runtime hatası oluşmaz.
5. TypeScript derleme hatası yoktur.
</success_criteria>

<output>
Tamamlandıktan sonra `.planning/phases/22-rapor-kalitesi-veri-zenginlestirme/22-03-SUMMARY.md` oluştur.
</output>
