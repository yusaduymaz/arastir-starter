---
phase: 22-rapor-kalitesi-veri-zenginlestirme
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/components/InvestmentCard.test.tsx
  - tests/lib/tcmb-fallback.test.ts
  - tests/lib/time-ago.test.ts
  - tests/lib/yahoo-client.test.ts
autonomous: true
requirements:
  - RPT-01
  - RPT-02
  - RPT-03
  - RPT-04

must_haves:
  truths:
    - "Tüm test dosyaları Wave 1 planları çalışmadan önce mevcut ve çalışabilir halde"
    - "InvestmentCard testleri null recommendation durumunu RED olarak işaretler (bileşen henüz null kabul etmiyor)"
    - "tcmb-fallback testleri getLatestNonNull davranışını kırmızıya düşürür"
    - "time-ago testleri timeAgo() fonksiyon sözleşmesini belgeler"
    - "yahoo-client testleri CompanyOverviewSchema'nın yeni alanları passthrough ile kabul ettiğini doğrular"
  artifacts:
    - path: "tests/components/InvestmentCard.test.tsx"
      provides: "RPT-01 null state doğrulaması — Wave 0 RED dosyası"
    - path: "tests/lib/tcmb-fallback.test.ts"
      provides: "RPT-02 getLatestNonNull sözleşme dosyası"
    - path: "tests/lib/time-ago.test.ts"
      provides: "RPT-03 timeAgo() sözleşme dosyası"
    - path: "tests/lib/yahoo-client.test.ts"
      provides: "RPT-04 CompanyOverviewSchema alan doğrulaması"
  key_links:
    - from: "tests/components/InvestmentCard.test.tsx"
      to: "src/components/dashboard/report/InvestmentCard.tsx"
      via: "import { InvestmentCard }"
      pattern: "InvestmentCard"
    - from: "tests/lib/tcmb-fallback.test.ts"
      to: "getLatestNonNull sözleşmesi"
      via: "inline pure function test"
      pattern: "getLatestNonNull"
    - from: "tests/lib/yahoo-client.test.ts"
      to: "src/types/market.ts"
      via: "import { CompanyOverviewSchema }"
      pattern: "CompanyOverviewSchema"
---

<objective>
Bu plan Wave 0 test dosyalarını oluşturur. Wave 1 (22-01, 22-02, 22-03) planları çalıştırılmadan önce
bu dört test dosyası commit edilmiş olmalı.

Amaç: TDD kırmızı-yeşil döngüsünü enforce etmek — her test önce başarısız çalışır (kırmızı),
Wave 1 planları implementasyonları teslim ettikten sonra yeşile döner.

Output:
- tests/components/InvestmentCard.test.tsx — RPT-01 null fallback testi
- tests/lib/tcmb-fallback.test.ts — RPT-02 getLatestNonNull pure function testi
- tests/lib/time-ago.test.ts — RPT-03 timeAgo() pure function testi
- tests/lib/yahoo-client.test.ts — RPT-04 CompanyOverviewSchema alan mapping testi
</objective>

<execution_context>
@C:/Users/duyma/Desktop/arastir-starter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/duyma/Desktop/arastir-starter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/duyma/Desktop/arastir-starter/.planning/ROADMAP.md
@C:/Users/duyma/Desktop/arastir-starter/.planning/STATE.md

<interfaces>
<!-- Mevcut InvestmentCard bileşeni (src/components/dashboard/report/InvestmentCard.tsx) -->
<!-- recommendation prop şu an: InvestmentRecommendation (null kabul etmiyor — Wave 0'da RED kalacak) -->
```typescript
interface InvestmentRecommendation {
  action: 'AL' | 'TUT' | 'SAT'
  score: number
  confidence: string
  shortTermOutlook: string
  longTermOutlook: string
  keyFactors: string[]
}

interface InvestmentCardProps {
  recommendation: InvestmentRecommendation  // Wave 0'da null kabul etmiyor
  ticker: string
}

export function InvestmentCard({ recommendation, ticker }: InvestmentCardProps)
```

<!-- Mevcut CompanyOverviewSchema (src/types/market.ts) -->
<!-- .passthrough() sayesinde bilinmeyen alanlar reddedilmez — test bunu kullanır -->
```typescript
export const CompanyOverviewSchema = z.object({
  Symbol: z.string(),
  Name: z.string().default(''),
  PERatio: z.string().default(''),
  EPS: z.string().default(''),
  // ... diğer alanlar ...
  '52WeekLow': z.string().default(''),
}).passthrough();  // PBRatio, Beta vb. passthrough ile geçer
// NOT: PBRatio, Beta, FloatShares, ROE, ROA, NetMargin 22-03 planı ile eklenecek
// Wave 0'da bu alanlar passthrough ile geçer ama default('') garantisi yoktur
```

<!-- EvdsDataPoint tipi (src/lib/tcmb/client.ts) -->
```typescript
export type EvdsDataPoint = z.infer<typeof EvdsDataPointSchema>;
// EvdsDataPointSchema:
//   Tarih: z.string()
//   TP_DK_USD_A: z.string().nullable().optional()
//   TP_FG_J0: z.string().nullable().optional()   // TÜFE
//   TP_MK_B_A2: z.string().nullable().optional() // Politika Faizi
//   .passthrough()

// getLatestNonNull Wave 0'da YOK — test inline tanımlar ve sözleşmeyi belgeler
```

<!-- Vitest yapılandırması (vitest.config.ts) -->
```typescript
// environment: 'node', globals: true
// @ alias → ./src
// Test ortamı: pure Node — jsdom KURULU DEĞİL
// React bileşen testleri için @testing-library/react gerekir — bu Wave 0'da mevcut olmayabilir
```
</interfaces>

<jsdom_note>
InvestmentCard bir React bileşenidir. Mevcut vitest.config.ts environment: 'node' kullanıyor.
@testing-library/react veya jsdom kurulu olmayabilir.

InvestmentCard testini yaz ANCAK dosyanın başına şu notu ekle:
  // NOTE: Bu test @testing-library/react ve jsdom gerektirir.
  // Ortam hazır değilse npx vitest run tests/components/ SKIP_JSDOM=1 ile atlanabilir.

Alternatif olarak: Bileşen import sınırını test etmek yerine InvestmentCard'ın
TypeScript kontratını (null kabul etmediği) test edebilirsin — bu durumda
tsc ile type-level test yaz (import ederek tip hatasını provoke et).

Executor iki yaklaşımdan birini seçer ve dosya başına hangi yaklaşımı kullandığını belgeler.
</jsdom_note>
</context>

<tasks>

<task type="auto">
  <name>Görev 1: tests/components/InvestmentCard.test.tsx</name>
  <files>tests/components/InvestmentCard.test.tsx</files>
  <read_first>
    - src/components/dashboard/report/InvestmentCard.tsx (tam dosya — prop interface ve render mantığı)
  </read_first>
  <action>
    tests/components/ dizinini oluştur (yoksa). Ardından tests/components/InvestmentCard.test.tsx
    dosyasını yaz.

    Dosyanın başına jsdom durumunu belgele: @testing-library/react ve jsdom kurulu mu diye
    kontrol et. Kurulu değilse testleri "skip" ile işaretle ve tsc type-level yaklaşımına geç.

    Tercih edilen yaklaşım — @testing-library/react KURULU DEĞİLSE:
    TypeScript tip sözleşmesini belgeleyen test yaz. Bileşeni import et, InvestmentCardProps
    tipini import et. Teste geçerli InvestmentRecommendation objesi ile tip uyumluluğu doğrula.
    Null geçildiğinde TypeScript'in bu durumu kabul etmediğini belgeleyen yorum satırları ekle.
    Tüm test bloklarını `it.todo()` veya `it.skip()` ile işaretle ve sözleşmeyi açıkla:

    ```
    describe('InvestmentCard', () => {
      it.todo('renders with full recommendation data (action=BUY equiv, confidence=0.85 equiv)')
      it.todo('renders with null recommendation — shows loading/placeholder, does not crash')
      it.todo('renders with minimal props')
    })
    ```

    Tercih edilen yaklaşım — @testing-library/react KURULU İSE:
    Üç test yaz:
    1. Tam recommendation (action: 'AL', score: 8.5, confidence: 'yüksek',
       shortTermOutlook: 'test', longTermOutlook: 'test', keyFactors: ['f1']) ile render —
       "AL (Buy)" metninin DOM'da mevcut olduğunu doğrula (getByText).
    2. null recommendation ile render — bileşen crash ETMEZ, "Yatırım tavsiyesi analiz edilemedi."
       veya "analiz" içeren bir metin bulunur.
       (Wave 0'da bu test BAŞARISIZ olacak — bileşen null kabul etmiyor; bu kasıtlı RED durumu)
    3. Minimal props (score: 5, confidence: 'orta', kısa outlook, boş keyFactors) ile render —
       bileşen crash etmez.

    Her iki yaklaşımda da dosya başına şu yorum satırını ekle:
    // WAVE 0 — Bu test dosyası 22-01-PLAN.md uygulanmadan önce RED durumdadır.
    // null recommendation testi 22-01 Görev 1 tamamlandıktan sonra yeşile döner.
  </action>
  <verify>
    <automated>npx vitest run tests/components/InvestmentCard.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Dosya tests/components/InvestmentCard.test.tsx konumunda mevcut
    - npx vitest run tests/components/InvestmentCard.test.tsx hata olmadan çalışır (skip/todo kabul edilebilir)
    - "WAVE 0" yorumu dosyanın başında mevcut
    - InvestmentCard import satırı dosyada mevcut
  </acceptance_criteria>
  <done>InvestmentCard test dosyası oluşturuldu; vitest run hata vermeden tamamlanıyor (skip/todo testler kabul edilebilir).</done>
</task>

<task type="auto">
  <name>Görev 2: tests/lib/tcmb-fallback.test.ts</name>
  <files>tests/lib/tcmb-fallback.test.ts</files>
  <read_first>
    - src/lib/tcmb/client.ts (EvdsDataPoint tipi ve EvdsDataPointSchema — getLatestNonNull'ın
      çalışacağı veri yapısını anlamak için)
  </read_first>
  <action>
    tests/lib/ dizinini oluştur (yoksa). Ardından tests/lib/tcmb-fallback.test.ts dosyasını yaz.

    getLatestNonNull HENÜZ src/ içinde tanımlı değil — 22-01-PLAN.md ile eklenecek.
    Bu nedenle test dosyasının içine pure function'ı inline tanımla (test sözleşmesini belgeler):

    ```typescript
    // WAVE 0 — Bu fonksiyon 22-01-PLAN.md Görev 2 ile history/[id]/page.tsx'e eklenecek.
    // Test sözleşmeyi belgeler ve implementasyon sonrası import ile güncellenecek.
    function getLatestNonNull(data: any[], field: string): string | null {
      if (!data || data.length === 0) return null;
      for (let i = data.length - 1; i >= 0; i--) {
        const val = data[i][field];
        if (val != null && val !== '' && val !== 'ND') return String(val);
      }
      return null;
    }
    ```

    Üç test yaz:

    1. 'getLatestNonNull(points, "TP_FG_J0") returns last non-null value from array'
       Input: [{ TP_FG_J0: '68.50' }, { TP_FG_J0: null }, { TP_FG_J0: '69.80' }]
       Expected: '69.80'

    2. 'returns null when all values are null'
       Input: [{ TP_FG_J0: null }, { TP_FG_J0: 'ND' }, { TP_FG_J0: '' }]
       Expected: null

    3. 'returns correct value when only last entry is valid'
       Input: [{ TP_FG_J0: null }, { TP_FG_J0: null }, { TP_FG_J0: '70.10' }]
       Expected: '70.10'

    Ek edge case testleri (opsiyonel ama önerilen):
    - Boş dizi: getLatestNonNull([], 'TP_FG_J0') → null
    - 'ND' değeri atlanır: [{ TP_FG_J0: '69.80' }, { TP_FG_J0: 'ND' }] → '69.80'
  </action>
  <verify>
    <automated>npx vitest run tests/lib/tcmb-fallback.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Dosya tests/lib/tcmb-fallback.test.ts konumunda mevcut
    - npx vitest run tests/lib/tcmb-fallback.test.ts çıkış kodu 0 (tüm testler geçiyor)
    - En az 3 test case mevcut (zorunlu 3 + opsiyonel edge case'ler)
    - getLatestNonNull inline tanımlı ve "WAVE 0" yorumu mevcut
  </acceptance_criteria>
  <done>tcmb-fallback test dosyası oluşturuldu; tüm testler yeşil (pure function inline tanımlı).</done>
</task>

<task type="auto">
  <name>Görev 3: tests/lib/time-ago.test.ts</name>
  <files>tests/lib/time-ago.test.ts</files>
  <read_first>
    - src/app/dashboard/history/[id]/page.tsx (timeAgo helper henüz yok — 22-02 ekleyecek;
      ancak RESEARCH.md Desen 3'teki sözleşmeyi referans al)
  </read_first>
  <action>
    tests/lib/time-ago.test.ts dosyasını yaz.

    timeAgo HENÜZ src/ içinde tanımlı değil — 22-02-PLAN.md ile eklenecek.
    Fonksiyonu inline tanımla (22-02'nin uygulayacağı sözleşmeyi belgeler):

    ```typescript
    // WAVE 0 — Bu fonksiyon 22-02-PLAN.md Görev 1 ile history/[id]/page.tsx'e eklenecek.
    // Test sözleşmeyi belgeler.
    function timeAgo(fetchedAt: number | string): string {
      const ts = typeof fetchedAt === 'string' ? new Date(fetchedAt).getTime() : fetchedAt;
      const diffMs = Date.now() - ts;
      const diffMin = Math.round(diffMs / 60000);
      if (diffMin < 1) return 'Az önce güncellendi';
      if (diffMin < 60) return `${diffMin} dakika önce güncellendi`;
      const diffHr = Math.round(diffMin / 60);
      return `${diffHr} saat önce güncellendi`;
    }
    ```

    Üç zorunlu test yaz:

    1. 'timeAgo(Date.now() - 60000) → "1 dakika önce güncellendi"'
       Input: Date.now() - 60_000 (1 dakika önce)
       Expected: '1 dakika önce güncellendi'

    2. 'timeAgo(Date.now() - 3600000) → "1 saat önce güncellendi"'
       Input: Date.now() - 3_600_000 (1 saat önce)
       Expected: '1 saat önce güncellendi'

    3. 'timeAgo(null) → "-"'
       NOT: null girişi için timeAgo sözleşmesi "-" dönmeli.
       Inline tanımı şu şekilde genişlet: fetchedAt null veya undefined ise return '-'
       (22-02 implementasyonu bu edge case'i de handle etmeli)

    Ek testler (önerilen):
    - timeAgo(Date.now() - 30_000) → 'Az önce güncellendi' (30 saniye önce)
    - timeAgo(Date.now() - 5 * 60_000) → '5 dakika önce güncellendi'
    - ISO string girişi: timeAgo(new Date(Date.now() - 120_000).toISOString()) → '2 dakika önce güncellendi'

    DIKKAT: Math.round kullandığı için kenar değerlerde (tam 60 dakika = 60 dakika mı yoksa 1 saat mi?)
    test hassasiyetine dikkat et. 3_600_000 ms tam 60 dakika = Math.round(60) = 60 dakika, 1 saat değil.
    Bunu düzelt: '1 saat önce güncellendi' için 3_660_000 ms (61 dakika) kullan.
  </action>
  <verify>
    <automated>npx vitest run tests/lib/time-ago.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Dosya tests/lib/time-ago.test.ts konumunda mevcut
    - npx vitest run tests/lib/time-ago.test.ts çıkış kodu 0 (tüm testler geçiyor)
    - En az 3 zorunlu test case mevcut
    - null girişi için "-" dönen edge case testi mevcut
    - "WAVE 0" yorumu mevcut
  </acceptance_criteria>
  <done>time-ago test dosyası oluşturuldu; tüm testler yeşil (pure function inline tanımlı, null edge case dahil).</done>
</task>

<task type="auto">
  <name>Görev 4: tests/lib/yahoo-client.test.ts</name>
  <files>tests/lib/yahoo-client.test.ts</files>
  <read_first>
    - src/types/market.ts (CompanyOverviewSchema tam tanımı — passthrough() mevcut; yeni alanlar 22-03 ile eklenecek)
    - src/lib/market/yahoo-client.ts (fmt() helper ve CompanyOverview mapping sözleşmesi)
  </read_first>
  <action>
    tests/lib/yahoo-client.test.ts dosyasını yaz.

    CompanyOverviewSchema'yı import et:
    ```typescript
    import { CompanyOverviewSchema } from '@/types/market';
    ```

    Üç zorunlu test yaz:

    1. 'CompanyOverviewSchema.parse({...}) accepts PBRatio, Beta, ROE, ROA, NetMargin, FloatShares fields'
       Input: {
         Symbol: 'THYAO',
         PBRatio: '2.5',
         Beta: '1.2',
         ROE: '23.45',
         ROA: '8.90',
         NetMargin: '12.34',
         FloatShares: '500000000'
       }
       Beklenti: parse() hata ATMAZ ve dönen obje Symbol === 'THYAO' içerir.
       NOT: .passthrough() sayesinde bu Wave 0'da geçer. 22-03 sonrası bu alanlar schema'da
       explicit olarak tanımlanır ve default('') güvencesi gelir.

    2. 'parse succeeds when these fields are absent (optional fields)'
       Input: { Symbol: 'THYAO' } (sadece zorunlu alan)
       Beklenti: parse() hata atmaz, result.Symbol === 'THYAO'

    3. 'ROE value 0.23 passes through as-is (not multiplied in schema)'
       Input: { Symbol: 'TEST', ROE: '0.23' }
       Beklenti: result.ROE === '0.23' (schema çarpma yapmaz — çarpma yahoo-client.ts'de yapılır)
       NOT: Bu test implementasyon sınırını belgeler: tipin içi değil, mapping katmanı dönüştürür.

    Dosyanın başına ekle:
    // WAVE 0 — CompanyOverviewSchema passthrough() kullandığı için bu testler Wave 0'da GEÇİYOR.
    // 22-03-PLAN.md Görev 1 tamamlandıktan sonra yeni alanlar explicit tanımlı olacak ve
    // default('') güvencesi eklenecek. Bu testler o noktada da geçmeye devam etmeli.
  </action>
  <verify>
    <automated>npx vitest run tests/lib/yahoo-client.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Dosya tests/lib/yahoo-client.test.ts konumunda mevcut
    - npx vitest run tests/lib/yahoo-client.test.ts çıkış kodu 0 (tüm testler geçiyor)
    - CompanyOverviewSchema import satırı mevcut: import { CompanyOverviewSchema } from '@/types/market'
    - En az 3 test case mevcut
    - ROE passthrough semantiğini belgeleyen yorum mevcut
    - "WAVE 0" yorumu mevcut
  </acceptance_criteria>
  <done>yahoo-client test dosyası oluşturuldu; tüm testler yeşil (passthrough sayesinde Wave 0'da geçiyor).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test dosyaları → src/ kodu | Test ortamı üretim kodunu import ediyor; side effect yok |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-00-01 | Information Disclosure | Test dosyalarında inline fonksiyon tanımları | accept | Test ortamı; prod'a dahil değil; CI sadece test çalıştırır |
| T-22-00-02 | Tampering | Inline fonksiyonların gerçek implementasyondan sapması | mitigate | WAVE 0 yorumları executor'ı 22-01 sonrası inline'ı import ile değiştirmeye yönlendirir |
</threat_model>

<verification>
1. `npx vitest run tests/` — tüm 4 test dosyası çalışır, kritik hatalar yoktur
2. `ls tests/components/InvestmentCard.test.tsx` — dosya mevcut
3. `ls tests/lib/tcmb-fallback.test.ts` — dosya mevcut
4. `ls tests/lib/time-ago.test.ts` — dosya mevcut
5. `ls tests/lib/yahoo-client.test.ts` — dosya mevcut
</verification>

<success_criteria>
1. 4 test dosyası oluşturulmuş ve commit edilmiş.
2. npx vitest run tests/ komutu hata olmadan tamamlanıyor (skip/todo kabul edilebilir; InvestmentCard null testi kırmızıda kalabilir).
3. tcmb-fallback, time-ago ve yahoo-client testleri Wave 0'da tamamen yeşil.
4. Her dosyada "WAVE 0" yorumu ve hangi plan tarafından gerçek implementasyona dönüştürüleceği belgelenmiş.
5. 22-01, 22-02, 22-03 planları bu dosyaları depends_on zinciriyle alıyor (wave: 0 → önce çalışır).
</success_criteria>

<output>
Tamamlandıktan sonra `.planning/phases/22-rapor-kalitesi-veri-zenginlestirme/22-00-SUMMARY.md` oluştur.
</output>
