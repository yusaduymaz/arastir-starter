---
phase: 01-foundation
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - "package.json"
  - "tsconfig.json"
  - "next.config.js"
  - "src/app/page.tsx"
  - ".gitignore"
autonomous: true
requirements:
  - "SETUP-01"

must_haves:
  truths:
    - "Next.js projesi yerel geliştirme sunucusunda başarıyla çalışır."
    - "TypeScript strict modu varsayılan olarak etkindir."
  artifacts:
    - path: "package.json"
      contains: '"next": "14"'
    - path: "tsconfig.json"
      contains: '"strict": true'
  key_links: []
---

<objective>
Next.js 14 projesini TypeScript ve Tailwind CSS desteğiyle başlatmak. Bu plan, projenin temelini oluşturur ve diğer tüm planlar bu altyapı üzerine inşa edilir.

Purpose: Projenin teknik iskeletini oluşturarak geliştirme ortamını hazır hale getirmek.
Output: Çalışan bir Next.js 14 uygulaması.
</objective>

<execution_context>
@C:/Users/duyma/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/duyma/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Next.js Projesini Oluşturma</name>
  <files>
    - "package.json"
    - "tsconfig.json"
    - "next.config.js"
    - "src/app/page.tsx"
    - ".gitignore"
  </files>
  <action>
    `create-next-app` komutunu kullanarak, kullanıcıya soru sormadan (`--no-stdin`) standart bir Next.js 14 projesi oluştur. Proje, TypeScript, Tailwind CSS, ESLint ve App Router kullanacak şekilde yapılandırılmalıdır.
  </action>
  <verify>
    <automated>
      npx create-next-app@latest . --ts --tailwind --eslint --app --no-src-dir --import-alias="@/*" --no-stdin
    </automated>
  </verify>
  <done>
    `create-next-app` komutu başarıyla tamamlanmalı ve proje dosyaları oluşturulmalıdır.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Projenin Çalıştığını Doğrulama</name>
  <files>
    - "package.json"
  </files>
  <action>
    Proje bağımlılıklarını `npm install` ile kur ve ardından `npm run build` komutuyla projenin başarılı bir şekilde derlendiğini doğrula. Bu, kurulumun doğru yapıldığını garanti eder.
  </action>
  <verify>
    <automated>
      npm install && npm run build
    </automated>
  </verify>
  <done>
    `npm run build` komutu hatasız bir şekilde tamamlanmalıdır.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Developer -> Local Machine | Developer makinesindeki kodun güvenilir olduğu varsayılır. |
| Local Machine -> NPM Registry | `npm install` sırasında indirilen paketlerin güvenilir olduğu varsayılır, ancak bu bir risk vektörüdür (supply chain attack). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-01 | Tampering | package.json | accept | Bu fazda, npm'den gelen paketlerin bütünlüğünü doğrulamak için ek bir mekanizma (örn: checksum kontrolü) kurulmayacaktır. Risk kabul edilmiştir. |
</threat_model>

<verification>
- `npm run dev` komutu çalıştırıldığında, uygulamanın `http://localhost:3000` adresinde sorunsuz açıldığı manuel olarak kontrol edilebilir.
</verification>

<success_criteria>
- Next.js projesi `npm run build` komutu ile başarıyla derlenir.
- `tsconfig.json` dosyasında `"strict": true` ayarı mevcuttur.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
