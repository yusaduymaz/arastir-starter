---
phase: 01-foundation
plan: 5
type: execute
wave: 4
depends_on:
  - "01-03"
files_modified:
  - ".env.example"
  - ".env.local"
  - "src/lib/tuik/client.ts"
  - "src/agents/test-tuik-agent.ts"
autonomous: false
requirements:
  - "DATA-02"

user_setup:
  - service: TÜİK Veri Portalı
    why: "Türkiye'nin demografik, ekonomik ve sosyal istatistiklerine erişim için resmi kaynak."
    env_vars: []
    dashboard_config:
      - task: "TÜİK veri portalının yapısını ve nasıl sorgu oluşturulduğunu inceleyin. Genellikle API anahtarı gerektirmez ama karmaşık sorgu URL'leri oluşturmak gerekir."
        location: "https://data.tuik.gov.tr/"

must_haves:
  truths:
    - "Bir test betiği, TÜİK veri portalından temel bir istatistik (örn: GSYİH) çekebilir."
  artifacts:
    - path: "src/lib/tuik/client.ts"
      provides: "TÜİK veri portalına istek gönderen fonksiyonlar"
    - path: "src/agents/test-tuik-agent.ts"
      provides: "TÜİK istemcisinin çalıştığını gösteren test betiği"
  key_links:
    - from: "src/agents/test-tuik-agent.ts"
      to: "src/lib/tuik/client.ts"
      via: "import { getTuikData }"
---

<objective>
Türkiye İstatistik Kurumu (TÜİK) veri portalı ile entegrasyonu sağlamak ve istatistiksel verileri çekmek.

Purpose: Projenin ikinci temel resmi veri kaynağını ekleyerek veri toplama yeteneklerini genişletmek.
Output: TÜİK'ten veri çekebilen bir istemci modülü ve bunu test eden bir betik.
</objective>

<execution_context>
@C:/Users/duyma/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/duyma/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TÜİK Veri İstemcisi Oluşturma</name>
  <files>
    - "src/lib/tuik/client.ts"
  </files>
  <behavior>
    - `getTuikData` fonksiyonu, bir sorgu veya veri kategorisi parametresi aldığında bir Promise dönmelidir.
    - TÜİK portalı genellikle karmaşık URL yapıları kullanır; fonksiyon bu URL'yi doğru bir şekilde oluşturmalıdır.
    - Hatalı bir istek durumunda (örn: 404), Promise reject olmalı veya bir hata fırlatmalıdır.
    - Başarılı bir istek, JSON veya CSV formatında veri döndürmelidir (API'nin formatına bağlı olarak).
  </behavior>
  <action>
    `src/lib/tuik/client.ts` içinde, TÜİK veri portalına istek atan bir fonksiyon oluştur. TÜİK'in doğrudan bir API anahtarı sistemi olmayabilir, bu nedenle fonksiyon, doğru yapılandırılmış sorgu URL'leri oluşturmaya odaklanmalıdır. `fetch` API'sini kullanarak GET isteği yap. Fonksiyonun amacı, belirli bir istatistiksel tabloyu çekmektir.
  </action>
  <verify>
    <automated>
      # Dosyanın varlığını kontrol et
      Test-Path "src/lib/tuik/client.ts"
    </automated>
  </verify>
  <done>
    `src/lib/tuik/client.ts` dosyası, TÜİK'ten veri çekmek için gerekli mantığı içeren, dışa aktarılmış bir fonksiyon barındırmaktadır.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: TÜİK Agent Test Betiği Oluşturma</name>
  <files>
    - "src/agents/test-tuik-agent.ts"
  </files>
  <action>
    Bir önceki adımda oluşturulan `getTuikData` fonksiyonunu çağıran basit bir test betiği oluştur. Bu betik, belirli bir istatistiksel veriyi (örn: GSYİH veya sektörel bir metrik) çekip konsola yazdıracak. Betiği `ts-node` veya benzeri bir araçla çalıştırılabilir şekilde tasarla.
  </action>
  <verify>
    <automated>
      # Dosyanın varlığını kontrol et
      Test-Path "src/agents/test-tuik-agent.ts"
    </automated>
  </verify>
  <done>
    `src/agents/test-tuik-agent.ts` dosyası oluşturulmuştur ve `getTuikData` fonksiyonunu çağırır.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: TÜİK Veri Akışını Doğrulama</name>
  <what-built>
    TÜİK API istemcisi ve test betiği oluşturuldu.
  </what-built>
  <how-to-verify>
    1. TÜİK veri portalından çekmek istediğiniz bir veri setinin sorgu URL'sini belirleyin: https://data.tuik.gov.tr/
    2. Gerekirse bu URL'yi `src/lib/tuik/client.ts` dosyasında güncelleyin.
    3. Proje bağımlılıkları arasına `ts-node` ekleyin (eğer önceki planda eklenmediyse): `npm install -D ts-node`
    4. Aşağıdaki komutu çalıştırın: `npx ts-node src/agents/test-tuik-agent.ts`
    5. Konsolda TÜİK'ten gelen istatistiksel verilerin (JSON, XML veya CSV formatında) listelendiğini doğrulayın. Hata mesajı almamalısınız.
  </how-to-verify>
  <resume-signal>
    Veriler başarıyla çekildiğinde "approved" yazın.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Application -> TÜİK Data Portal | Uygulama, TÜİK'in resmi portalına güvenli (HTTPS) bir bağlantı üzerinden erişir. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-07 | Tampering | Data Integrity | accept | TÜİK'ten gelen verinin bütünlüğü, HTTPS ile korunmaktadır. Uygulama katmanında ek bir checksum veya imza doğrulaması bu faz için yapılmayacaktır. Verinin kaynağına güvenilmektedir. |
| T-01-08 | Information Disclosure | Query Parameters | accept | Sorgu parametreleri hassas bilgi içermez; halka açık istatistikleri hedefler. Risk kabul edilmiştir. |
</threat_model>

<verification>
- `npx ts-node src/agents/test-tuik-agent.ts` komutu çalıştırıldığında, konsola hata yerine TÜİK'ten gelen veriler yazdırılmalıdır.
</verification>

<success_criteria>
- Bir betik veya ajan, TÜİK veri portalından istatistiksel verileri başarıyla çekip konsola yazdırabilir.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-05-SUMMARY.md`
</output>
