---
phase: 01-foundation
plan: 3
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - "package.json"
  - ".env.example"
  - ".env.local"
  - ".gitignore"
  - "src/lib/supabase/client.ts"
  - "supabase/schema.sql"
autonomous: false
requirements:
  - "SETUP-02"

user_setup:
  - service: Supabase
    why: "Uygulamanın veritabanı ve kimlik doğrulama sağlayıcısı."
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Supabase Proje Ayarları > API > Proje URL"
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: "Supabase Proje Ayarları > API > Proje API Anahtarları > anon (public)"
    dashboard_config:
      - task: "Yeni bir Supabase projesi oluşturun."
        location: "https://app.supabase.com/"

must_haves:
  truths:
    - "Uygulama, Supabase istemcisini başarıyla başlatabilir."
    - "Veritabanı şeması, araştırma geçmişini saklamak için bir tablo içerir."
  artifacts:
    - path: "src/lib/supabase/client.ts"
      provides: "Supabase istemci örneği"
    - path: "supabase/schema.sql"
      contains: "CREATE TABLE research_history"
    - path: ".env.local"
      provides: "Supabase bağlantı bilgileri"
  key_links:
    - from: "Any component/agent"
      to: "src/lib/supabase/client.ts"
      via: "import { supabase } from '@/lib/supabase/client'"
---

<objective>
Supabase'i projeye entegre etmek, veritabanı bağlantısını kurmak ve araştırma geçmişi için temel veritabanı şemasını oluşturmak.

Purpose: Uygulamanın stateful (durum bilgisi olan) verileri (araştırma geçmişi, kullanıcılar) saklayabilmesi için kalıcı bir depolama katmanı sağlamak.
Output: Yapılandırılmış bir Supabase istemcisi ve temel veritabanı şeması.
</objective>

<execution_context>
@C:/Users/duyma/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/duyma/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Supabase Kütüphanesini Yükleme</name>
  <files>
    - "package.json"
  </files>
  <action>
    Supabase ile etkileşim kurmak için gerekli olan JavaScript istemci kütüphanesini projeye ekle.
  </action>
  <verify>
    <automated>
      npm install @supabase/supabase-js
    </automated>
  </verify>
  <done>
    `@supabase/supabase-js` paketi `package.json` dosyasındaki `dependencies` listesine eklenmiştir.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Ortam Değişkenlerini Yapılandırma</name>
  <files>
    - ".env.example"
    - ".gitignore"
  </files>
  <action>
    Supabase Proje URL'si ve Anon Anahtarı için bir `.env.example` dosyası oluştur. Hassas bilgileri içeren `.env.local` dosyasının versiyon kontrolüne eklenmemesi için `.gitignore` dosyasını güncelle.
  </action>
  <verify>
    <automated>
      echo "NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=" > .env.example && echo "
# Local Environment Variables
.env.local" >> .gitignore
    </automated>
  </verify>
  <done>
    `.env.example` dosyası oluşturulmuş ve `.gitignore` dosyası `.env.local`'ı içerecek şekilde güncellenmiştir.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Supabase İstemcisini Oluşturma</name>
  <files>
    - "src/lib/supabase/client.ts"
  </files>
  <action>
    Ortam değişkenlerinden (`process.env`) URL ve anon anahtarını okuyarak Supabase istemcisini başlatan bir TypeScript dosyası oluştur. Bu istemci, uygulama genelinde yeniden kullanılabilir bir singleton olarak dışa aktarılmalıdır.
  </action>
  <verify>
    <automated>
      # Dosyanın oluşturulduğunu ve 'createClient' içerdiğini kontrol et
      (Test-Path "src/lib/supabase/client.ts") -and (Select-String -Path "src/lib/supabase/client.ts" -Pattern "createClient" -Quiet)
    </automated>
  </verify>
  <done>
    `src/lib/supabase/client.ts` dosyası, dışa aktarılmış bir `supabase` istemci örneği içermektedir.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Araştırma Geçmişi Şemasını Oluşturma</name>
  <files>
    - "supabase/schema.sql"
  </files>
  <action>
    `supabase` klasörü altında, `research_history` adında bir tablo oluşturan bir SQL betiği oluştur. Bu tablo en azından `id`, `created_at`, `query` (kullanıcı sorgusu), ve `result_path` (çıktı dosyasının yolu) sütunlarını içermelidir.
  </action>
  <verify>
    <automated>
      # Dosyanın oluşturulduğunu ve 'CREATE TABLE research_history' içerdiğini kontrol et
      (Test-Path "supabase/schema.sql") -and (Select-String -Path "supabase/schema.sql" -Pattern "CREATE TABLE research_history" -Quiet)
    </automated>
  </verify>
  <done>
    `supabase/schema.sql` dosyası, `research_history` tablosunu tanımlayan geçerli bir SQL `CREATE TABLE` ifadesi içermektedir.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Supabase Bağlantısını Doğrulama</name>
  <what-built>
    Supabase istemcisi ve ortam değişkenleri yapılandırıldı. Proje şeması oluşturuldu.
  </what-built>
  <how-to-verify>
    1. Supabase projenizi oluşturun: https://app.supabase.com/
    2. Proje Ayarları > API menüsünden "Project URL" ve "anon public" anahtarını alın.
    3. Proje kök dizininde `.env.local` adında bir dosya oluşturun.
    4. İçeriğini şu şekilde doldurun:
       NEXT_PUBLIC_SUPABASE_URL=YOUR_PROJECT_URL
       NEXT_PUBLIC_SUPABASE_ANON_KEY=YOUR_ANON_KEY
    5. Supabase projenizdeki "Table Editor" bölümüne gidin ve `supabase/schema.sql` dosyasındaki SQL'i çalıştırarak `research_history` tablosunu oluşturun.
    6. `npm run dev` ile uygulamayı başlatın. Konsolda Supabase ile ilgili bir bağlantı hatası olmadığını teyit edin.
  </how-to-verify>
  <resume-signal>
    Doğrulama tamamlandığında ve bağlantı başarılı olduğunda "approved" yazın.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Application -> Supabase | Uygulama, Supabase API'sine güvenli (HTTPS) bir bağlantı üzerinden erişir. |
| Developer -> Supabase Dashboard | Developer'ın Supabase proje ayarlarına tam erişimi vardır. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-03 | Information Disclosure | NEXT_PUBLIC_SUPABASE_ANON_KEY | accept | Anon key'in public olması Supabase'in çalışma modelinin bir parçasıdır. Güvenlik, Row Level Security (RLS) ile sağlanır. Bu fazda RLS henüz etkinleştirilmemiştir. |
| T-01-04 | Elevation of Privilege | Supabase Dashboard Access | accept | Proje erişimi yalnızca yetkili geliştiricilerle sınırlıdır. Bu risk, organizasyonel bir kontrol olarak kabul edilir. |
</threat_model>

<verification>
- `src/lib/supabase/client.ts` dosyasından `supabase` istemcisi içe aktarılıp basit bir sorgu (örn: `supabase.from('research_history').select('*').limit(1)`) çalıştırıldığında hata alınmamalıdır.
</verification>

<success_criteria>
- Next.js uygulaması yerel olarak çalışır ve Supabase veritabanına başarılı bir şekilde bağlanır.
- `research_history` tablosu Supabase veritabanında oluşturulmuştur.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
