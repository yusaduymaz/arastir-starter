---
phase: 01-foundation
plan: 2
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - "src/agents/.gitkeep"
  - "src/lib/.gitkeep"
  - "src/components/.gitkeep"
  - "src/types/.gitkeep"
  - "outputs/.gitkeep"
  - "research/.gitkeep"
  - "supabase/.gitkeep"
autonomous: true
requirements:
  - "SETUP-03"

must_haves:
  truths:
    - "Proje, GEMINI.md'de belirtilen standart klasör yapısına sahiptir."
  artifacts:
    - path: "src/agents"
    - path: "src/lib"
    - path: "outputs"
    - path: "research"
    - path: "supabase"
  key_links: []
---

<objective>
`GEMINI.md` belgesinde tanımlanan standart proje klasör yapısını oluşturmak. Bu, kodun organize ve ölçeklenebilir olmasını sağlar.

Purpose: Projenin en başından itibaren tutarlı bir yapıya sahip olmasını sağlamak.
Output: Gerekli tüm klasörlerin oluşturulmuş ve `.gitkeep` dosyalarıyla versiyon kontrolüne hazır hale getirilmiş olması.
</objective>

<execution_context>
@C:/Users/duyma/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/duyma/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Gerekli Klasörleri Oluşturma</name>
  <files>
    - "src/agents/.gitkeep"
    - "src/lib/.gitkeep"
    - "src/components/.gitkeep"
    - "src/types/.gitkeep"
    - "outputs/.gitkeep"
    - "research/.gitkeep"
    - "supabase/.gitkeep"
  </files>
  <action>
    Proje ana dizininde ve `src` altında `GEMINI.md`'de belirtilen tüm klasörleri (`agents`, `lib`, `components`, `types`, `outputs`, `research`, `supabase`) oluştur. Git'in boş klasörleri takip etmesini sağlamak için her birinin içine boş bir `.gitkeep` dosyası ekle.
  </action>
  <verify>
    <automated>
      $folders = @("src/agents", "src/lib", "src/components", "src/types", "outputs", "research", "supabase"); foreach ($folder in $folders) { New-Item -ItemType Directory -Force -Path $folder; New-Item -ItemType File -Force -Path (Join-Path $folder ".gitkeep") }
    </automated>
  </verify>
  <done>
    Tüm belirtilen klasörler ve içlerindeki `.gitkeep` dosyaları başarıyla oluşturulmuştur. `ls -R` komutu çıktısında bu klasörler görünmelidir.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Developer -> Local Machine | Klasör yapısı developer tarafından tanımlandığı için güvenilirdir. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-02 | Information Disclosure | N/A | accept | Bu plan sadece boş klasörler oluşturduğu için bir risk barındırmamaktadır. |
</threat_model>

<verification>
- `ls -R` komutu çalıştırıldığında, `src/agents`, `src/lib`, `outputs`, `research` ve `supabase` klasörlerinin listelendiği doğrulanmalıdır.
</verification>

<success_criteria>
- Proje kök dizininde `outputs`, `research`, `supabase` klasörleri mevcuttur.
- `src` dizini altında `agents`, `lib`, `components`, `types` klasörleri mevcuttur.
- Oluşturulan her klasörün içinde bir `.gitkeep` dosyası bulunur.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
