---
phase: 01-foundation
plan: 4
type: execute
wave: 4
depends_on:
  - "01-03"
files_modified:
  - ".env.example"
  - ".env.local"
  - "src/lib/tcmb/client.ts"
  - "src/agents/test-tcmb-agent.ts"
autonomous: false
requirements:
  - "DATA-01"

user_setup:
  - service: TCMB EVDS
    why: "Türkiye'nin makroekonomik verilerine (enflasyon, faiz vb.) erişim için resmi kaynak."
    env_vars:
      - name: TCMB_API_KEY
        source: "https://evds2.tcmb.gov.tr/ > Profil > API Anahtarı"
    dashboard_config:
      - task: "EVDS sistemine kayıt olup bir API anahtarı alın."
        location: "https://evds2.tcmb.gov.tr/"

must_haves:
  truths:
    - "Bir test betiği, TCMB EVDS API'sinden güncel enflasyon verisini çekebilir."
  artifacts:
    - path: "src/lib/tcmb/client.ts"
      provides: "TCMB API'sine istek gönderen fonksiyonlar"
    - path: "src/agents/test-tcmb-agent.ts"
      provides: "TCMB istemcisinin çalıştığını gösteren test betiği"
  key_links:
    - from: "src/agents/test-tcmb-agent.ts"
      to: "src/lib/tcmb/client.ts"
      via: "import { getTcmbData }"
---

<objective>
Türkiye Cumhuriyet Merkez Bankası (TCMB) Elektronik Veri Dağıtım Sistemi (EVDS) API'si ile entegrasyonu sağlamak ve makroekonomik verileri çekmek.

Purpose: Projenin harici bir resmi API'den veri çekme yeteneğini kanıtlamak ve temel veri kaynaklarından birini eklemek.
Output: TCMB API'sinden veri çekebilen bir istemci modülü ve bunu test eden bir betik.
</objective>

<execution_context>
@C:/Users/duyma/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/duyma/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TCMB Veri İstemcisi Oluşturma</name>
  <files>
    - "src/lib/tcmb/client.ts"
    - ".env.example"
  </files>
  <behavior>
    - `getTcmbData` fonksiyonu, seri kodu ve tarih aralığı verildiğinde bir Promise dönmelidir.
    - API anahtarı ortam değişkeninden (`TCMB_API_KEY`) okunmalıdır.
    - Hatalı bir istek durumunda (örn: geçersiz API anahtarı), Promise reject olmalı veya bir hata fırlatmalıdır.
    - Başarılı bir istek, JSON formatında veri döndürmelidir.
  </behavior>
  <action>
    `src/lib/tcmb/client.ts` içinde, TCMB EVDS API'sine istek atan bir fonksiyon oluştur. Bu fonksiyon, parametre olarak veri serisini (örn: `TP.DK.USD.A` - Dolar Kuru) ve tarih aralığını almalıdır. API anahtarını `process.env.TCMB_API_KEY` üzerinden okumalıdır. `fetch` API'sini kullanarak GET isteği yap. Ayrıca, `.env.example` dosyasına `TCMB_API_KEY=` satırını ekle.
  </action>
  <verify>
    <automated>
      # Dosyanın varlığını kontrol et
      Test-Path "src/lib/tcmb/client.ts"
    </automated>
  </verify>
  <done>
    `src/lib/tcmb/client.ts` dosyası, TCMB API'sinden veri çekmek için gerekli mantığı içeren, dışa aktarılmış bir fonksiyon barındırmaktadır.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: TCMB Agent Test Betiği Oluşturma</name>
  <files>
    - "src/agents/test-tcmb-agent.ts"
  </files>
  <action>
    Bir önceki adımda oluşturulan `getTcmbData` fonksiyonunu çağıran basit bir test betiği oluştur. Bu betik, belirli bir makroekonomik veri serisini (örn: TÜFE - Tüketici Fiyat Endeksi) çekip konsola yazdıracak. Betiği `ts-node` veya benzeri bir araçla çalıştırılabilir şekilde tasarla.
  </action>
  <verify>
    <automated>
      # Dosyanın varlığını kontrol et
      Test-Path "src/agents/test-tcmb-agent.ts"
    </automated>
  </verify>
  <done>
    `src/agents/test-tcmb-agent.ts` dosyası oluşturulmuştur ve `getTcmbData` fonksiyonunu çağırır.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: TCMB Veri Akışını Doğrulama</name>
  <what-built>
    TCMB API istemcisi ve test betiği oluşturuldu.
  </what-built>
  <how-to-verify>
    1. TCMB EVDS sitesinden API anahtarınızı alın: https://evds2.tcmb.gov.tr/
    2. `.env.local` dosyanıza `TCMB_API_KEY=YOUR_API_KEY` satırını ekleyin.
    3. Proje bağımlılıkları arasına `ts-node` ekleyin: `npm install -D ts-node`
    4. Aşağıdaki komutu çalıştırın: `npx ts-node src/agents/test-tcmb-agent.ts`
    5. Konsolda TCMB'den gelen makroekonomik verilerin (JSON formatında) listelendiğini doğrulayın. Hata mesajı almamalısınız.
  </how-to-verify>
  <resume-signal>
    Veriler başarıyla çekildiğinde "approved" yazın.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Application -> TCMB API | Uygulama, TCMB'nin resmi API'sine güvenli (HTTPS) bir bağlantı üzerinden erişir. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-05 | Information Disclosure | TCMB_API_KEY | mitigate | API anahtarı, `.env.local` dosyasında saklanır ve bu dosya `.gitignore` ile versiyon kontrolü dışında tutulur. Sunucu tarafı işlemlerde kullanılarak ifşa riski azaltılır. |
| T-01-06 | Denial of Service | API Rate Limiting | accept | TCMB API'sinin kendi rate limitleri vardır. Bu fazda, uygulama tarafında ek bir rate limiting mekanizması kurulmayacaktır. Risk kabul edilmiştir. |
</threat_model>

<verification>
- `npx ts-node src/agents/test-tcmb-agent.ts` komutu çalıştırıldığında, konsola hata yerine TCMB'den gelen veriler yazdırılmalıdır.
</verification>

<success_criteria>
- Bir betik veya ajan, TCMB EVDS API'sinden makroekonomik verileri başarıyla çekip konsola yazdırabilir.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
