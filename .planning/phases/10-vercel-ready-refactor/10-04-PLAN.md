---
phase: "10"
plan: "04"
type: execute
wave: 3
depends_on: ["10-01", "10-02", "10-03"]
files_modified: ["src/app/dashboard/history/[id]/page.tsx", "src/types/research.ts", "src/components/dashboard/ActivePipeline.tsx"]
autonomous: true
requirements: ["UI-06", "VERCEL-03"]

must_haves:
  truths:
    - "Report detail page displays market_data section when available (price, change, sector)."
    - "Report detail page displays macro_data section when available (USD/TRY, TÜFE, faiz)."
    - "Report detail page gracefully handles missing data sections (no blank boxes or N/A spam)."
    - "ActivePipeline shows correct agent status for topic queries (market agent skipped, not failed)."
---

<objective>
Update the frontend report detail page to render the newly stored market_data and macro_data from Supabase columns. Currently the report detail page only shows kap_data, news_data, and synthesis_data. The new DB columns from Plan 10-01 need corresponding UI sections.
</objective>

<context>
After Plan 10-01, the research_history table has these JSONB columns:
- `kap_data` — KAP disclosures array
- `news_data` — news articles array  
- `market_data` — stock quote, overview, monthly series (NEW)
- `macro_data` — TCMB EVDS data points array (NEW)
- `synthesis_data` — report data with executive summary, risks, opportunities

The report detail page at `src/app/dashboard/history/[id]/page.tsx` needs to:
1. Display market data (price, change, P/E, sector) in a dedicated card
2. Display macro data (USD/TRY, TÜFE, faiz trends) in a summary card
3. Handle topic queries gracefully (where market_data is null)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add market_data display card</name>
  <files>src/app/dashboard/history/[id]/page.tsx</files>
  <read_first>src/app/dashboard/history/[id]/page.tsx</read_first>
  <action>
    In the left column (alongside Sentez Özeti), add a Piyasa Verisi card that shows:
    - Current price + daily change (with color: green for positive, red for negative)
    - 52-week high/low
    - P/E ratio, market cap
    - Sector / Industry
    
    Only render this card when `market_data` is non-null. Skip entirely for topic queries.
  </action>
  <acceptance_criteria>
    - THYAO report shows a Piyasa Verisi card with real price data.
    - "Enerji Sektoru" report does NOT show a Piyasa Verisi card (no empty/N/A card).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add macro_data display card</name>
  <files>src/app/dashboard/history/[id]/page.tsx</files>
  <action>
    Add a Makroekonomik Göstergeler card showing:
    - Latest USD/TRY rate
    - Latest TÜFE (CPI) value
    - Latest policy interest rate
    - Mini trend: last 3 months values
    
    Only render when `macro_data` is non-null and has entries.
  </action>
  <acceptance_criteria>
    - Report shows macro indicators when macro agent succeeded.
    - Card is omitted when macro_data is null/empty.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Update ResearchSession type for new columns</name>
  <files>src/types/research.ts</files>
  <read_first>src/types/research.ts</read_first>
  <action>
    Add `market_data` and `macro_data` optional fields to `ResearchSession` interface to match the new Supabase columns:
    ```typescript
    market_data?: any;
    macro_data?: any;
    ```
  </action>
  <acceptance_criteria>
    - ResearchSession includes market_data and macro_data fields.
    - No TypeScript errors when accessing these fields.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Handle skipped agents in ActivePipeline</name>
  <files>src/components/dashboard/ActivePipeline.tsx</files>
  <read_first>src/components/dashboard/ActivePipeline.tsx</read_first>
  <action>
    When the orchestrator logs "market agent skipped" for topic queries, the ActivePipeline grid should show the market agent as "skipped" (gray, dash icon) rather than "idle". Add a `'skipped'` status to the STATUS_ICON map.
  </action>
  <acceptance_criteria>
    - Topic queries show market agent as skipped (not idle or failed).
    - Ticker queries show all agents normally.
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. Navigate to a completed THYAO report → market data card shows price, P/E, sector.
2. Navigate to a topic query report → no market data card, no N/A values.
3. Macro data card shows USD/TRY, TÜFE, faiz values.
4. TypeScript compiles cleanly.
</verification>
